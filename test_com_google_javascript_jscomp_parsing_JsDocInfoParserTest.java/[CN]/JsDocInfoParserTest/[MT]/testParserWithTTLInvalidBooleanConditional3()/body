{
  parse("@template T := cond(eq(T, foo()), R, S) =: */","Bad type annotation. Invalid type expression","Bad type annotation. Invalid expression inside boolean","Bad type annotation. Invalid expression inside conditional");
}
