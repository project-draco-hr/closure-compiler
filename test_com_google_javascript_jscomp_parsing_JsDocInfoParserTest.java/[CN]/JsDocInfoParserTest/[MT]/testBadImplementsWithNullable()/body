{
  JSDocInfo jsdoc=parse("@implements {Disposable?}\n * @constructor */","expected closing }");
  assertTrue(jsdoc.isConstructor());
  assertTypeEquals(new NamedType(registry,"Disposable",null,-1,-1),jsdoc.getImplementedInterfaces().get(0));
}
