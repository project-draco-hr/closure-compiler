{
  JSDocInfo info=parse("@type {!Array.<(number,\n* null)>}*/","Bad type annotation. union types should use | instead of ,");
  assertTypeEquals(createTemplatizedType(ARRAY_TYPE,createUnionType(NUMBER_TYPE,NULL_TYPE)),info.getType());
}
