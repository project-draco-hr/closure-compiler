{
  JSDocInfo jsdoc=parse("@constructor\n * @extends {Object?} */","Bad type annotation. expected closing }");
  assertThat(jsdoc.isConstructor()).isTrue();
  assertTypeEquals(OBJECT_TYPE,jsdoc.getBaseType());
}
