{
  JSDocInfo info=parse("@type {(string?,boolean)}*/","Bad type annotation. union types should use | instead of ,");
  assertTypeEquals(createUnionType(createNullableType(STRING_TYPE),BOOLEAN_TYPE),info.getType());
}
