{
  parse("@template T := cond(isDefined('foo'), R, S) =: */","Bad type annotation. Invalid name","Bad type annotation. Invalid expression inside boolean","Bad type annotation. Invalid expression inside conditional");
}
