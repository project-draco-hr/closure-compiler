{
  super.setUp();
  extraAnnotations=Sets.newHashSet(ParserRunner.createConfig(true,false).annotationNames.keySet());
  extraSuppressions=Sets.newHashSet(ParserRunner.createConfig(true,false).suppressionNames);
  extraSuppressions.add("x");
  extraSuppressions.add("y");
  extraSuppressions.add("z");
}
