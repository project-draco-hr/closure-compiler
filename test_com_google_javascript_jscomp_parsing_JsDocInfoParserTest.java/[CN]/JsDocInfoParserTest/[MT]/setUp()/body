{
  super.setUp();
  extraAnnotations=Sets.newHashSet(ParserRunner.createConfig(true).annotationNames.keySet());
  extraSuppressions=Sets.newHashSet(ParserRunner.createConfig(true).suppressionNames);
  extraSuppressions.add("x");
  extraSuppressions.add("y");
  extraSuppressions.add("z");
}
