{
  super.setUp();
  fileLevelJsDocBuilder=null;
  extraAnnotations=new HashSet<>(ParserRunner.createConfig(true,LanguageMode.ECMASCRIPT3,null).annotationNames.keySet());
  extraSuppressions=new HashSet<>(ParserRunner.createConfig(true,LanguageMode.ECMASCRIPT3,null).suppressionNames);
  extraSuppressions.add("x");
  extraSuppressions.add("y");
  extraSuppressions.add("z");
}
