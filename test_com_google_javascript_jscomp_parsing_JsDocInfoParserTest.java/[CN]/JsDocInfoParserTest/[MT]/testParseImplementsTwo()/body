{
  List<JSTypeExpression> interfaces=parse("* @implements {SomeInterface1}\n" + "* @implements {SomeInterface2}\n" + "*/").getImplementedInterfaces();
  assertEquals(2,interfaces.size());
  assertTypeEquals(new NamedType(registry,"SomeInterface1",null,-1,-1),interfaces.get(0));
  assertTypeEquals(new NamedType(registry,"SomeInterface2",null,-1,-1),interfaces.get(1));
}
