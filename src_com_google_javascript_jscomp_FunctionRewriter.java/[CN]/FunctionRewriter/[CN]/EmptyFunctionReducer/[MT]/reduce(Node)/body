{
  if (!NodeUtil.isFunctionExpression(node)) {
    return node;
  }
  if (isEmptyFunction(node)) {
    return buildCallNode(FACTORY_METHOD_NAME,null,node.getLineno(),node.getCharno());
  }
 else {
    return node;
  }
}
