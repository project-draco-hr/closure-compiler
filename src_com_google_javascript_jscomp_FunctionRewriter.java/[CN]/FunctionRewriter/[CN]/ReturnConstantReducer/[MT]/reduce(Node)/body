{
  if (!NodeUtil.isAnonymousFunction(node)) {
    return node;
  }
  Node valueNode=getValueNode(node);
  if (valueNode != null) {
    return buildCallNode(FACTORY_METHOD_NAME,valueNode,node.getLineno(),node.getCharno());
  }
 else {
    return node;
  }
}
