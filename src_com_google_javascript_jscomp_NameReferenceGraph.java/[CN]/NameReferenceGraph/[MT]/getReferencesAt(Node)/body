{
  Preconditions.checkArgument(site.isGetProp() || site.isName());
  List<Name> result=new ArrayList<>();
  for (  Name target : referenceMap.get(site)) {
    result.add(target);
  }
  return result;
}
