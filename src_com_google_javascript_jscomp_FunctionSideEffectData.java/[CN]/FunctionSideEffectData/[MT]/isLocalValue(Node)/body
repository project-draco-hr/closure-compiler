{
switch (value.getType()) {
case Token.NAME:
    String name=value.getString();
  return getScope().isDeclared(name,false) && !nonLocalValues.contains(new NameValueEntry(value));
case Token.CALL:
return !nonLocalValues.contains(new CallValueEntry(value));
}
return false;
}
