{
switch (n.getType()) {
case Token.CASE:
case Token.FOR:
case Token.FUNCTION:
case Token.HOOK:
case Token.IF:
case Token.SWITCH:
case Token.WHILE:
    if (entering) {
      assignAllowed.add(0);
    }
 else {
      assignAllowed.remove();
    }
  break;
}
}
