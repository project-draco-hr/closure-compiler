{
  SimpleDependencyInfo a=new SimpleDependencyInfo("a","a",provides("a"),requires("a"));
  try {
    new SortedDependencies<SimpleDependencyInfo>(Lists.newArrayList(a));
    fail("expected exception");
  }
 catch (  CircularDependencyException e) {
    assertEquals("a -> a",e.getMessage());
  }
}
