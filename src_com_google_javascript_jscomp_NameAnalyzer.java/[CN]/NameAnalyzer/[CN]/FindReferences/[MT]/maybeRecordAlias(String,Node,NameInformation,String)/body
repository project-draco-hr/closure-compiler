{
  if ((parent.getType() == Token.NAME || parent.getType() == Token.ASSIGN) && referring != null && scopes.get(parent) == referring) {
    recordAlias(referringName,name);
    return true;
  }
  return false;
}
