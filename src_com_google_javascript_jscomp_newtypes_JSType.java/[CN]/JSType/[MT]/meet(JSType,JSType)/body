{
  if (lhs.isTop()) {
    return rhs;
  }
 else   if (rhs.isTop()) {
    return lhs;
  }
 else   if (lhs.isUnknown()) {
    return rhs;
  }
 else   if (rhs.isUnknown()) {
    return lhs;
  }
  String typeVar=lhs.typeVar != null && lhs.typeVar.equals(rhs.typeVar) ? lhs.typeVar : null;
  return new JSType(lhs.mask & rhs.mask,null,ObjectType.meetSets(lhs.objs,rhs.objs),typeVar);
}
