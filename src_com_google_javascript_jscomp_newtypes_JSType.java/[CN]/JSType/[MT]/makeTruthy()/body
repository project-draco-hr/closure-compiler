{
  if (this.isTop()) {
    return this;
  }
  if (this.isUnknown()) {
    return TRUTHY;
  }
  return makeType(getMask() & ~NULL_MASK & ~FALSE_MASK& ~UNDEFINED_MASK,getObjs(),getTypeVar(),getEnums());
}
