{
  Preconditions.checkState(getObjs() != null && getLocation() == null);
  if (type == null && isConstant) {
    type=JSType.UNKNOWN;
  }
  return new UnionType(getMask(),null,ObjectType.withDeclaredProperty(getObjs(),qname,type,isConstant),getTypeVar(),getEnums());
}
