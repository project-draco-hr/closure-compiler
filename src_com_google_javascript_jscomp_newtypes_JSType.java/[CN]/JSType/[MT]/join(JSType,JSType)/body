{
  if (lhs.isTop() || rhs.isTop()) {
    return TOP;
  }
 else   if (lhs.isUnknown() || rhs.isUnknown()) {
    return UNKNOWN;
  }
  Preconditions.checkState(lhs.typeVar == null || rhs.typeVar == null || lhs.typeVar.equals(rhs.typeVar));
  return new JSType(lhs.mask | rhs.mask,Objects.equal(lhs.location,rhs.location) ? lhs.location : null,ObjectType.joinSets(lhs.objs,rhs.objs),lhs.typeVar != null ? lhs.typeVar : rhs.typeVar);
}
