{
  if (isBottom() || isUnknown()) {
    return UNKNOWN;
  }
  Preconditions.checkState(getObjs() != null || getEnums() != null);
  return nullAcceptingJoin(TypeWithProperties.getProp(getObjs(),qname),TypeWithProperties.getProp(getEnums(),qname));
}
