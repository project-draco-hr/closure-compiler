{
  if (isBottom() || isUnknown()) {
    return UNKNOWN;
  }
  Preconditions.checkState(objs != null || enums != null);
  return nullAcceptingJoin(TypeWithProperties.getProp(objs,qname),TypeWithProperties.getProp(enums,qname));
}
