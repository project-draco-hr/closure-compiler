{
  if (getObjs() == null) {
    Preconditions.checkState(getEnums() != null);
    return this;
  }
  return new UnionType(getMask(),getLocation(),ObjectType.withLooseObjects(getObjs()),getTypeVar(),getEnums());
}
