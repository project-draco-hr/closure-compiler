{
  if (Objects.equal(location,this.location)) {
    return this;
  }
  String newLocation=location == null ? null : JSType.GENERIC_LOCATION;
  return new JSType(mask,location,objs == null ? null : ObjectType.withLocation(objs,newLocation),typeVar);
}
