{
  if (Objects.equal(location,getLocation())) {
    return this;
  }
  String newLoc=location == null ? null : JSType.GENERIC_LOCATION;
  return new UnionType(getMask(),location,getObjs() == null ? null : ObjectType.withLocation(getObjs(),newLoc),getTypeVar(),getEnums());
}
