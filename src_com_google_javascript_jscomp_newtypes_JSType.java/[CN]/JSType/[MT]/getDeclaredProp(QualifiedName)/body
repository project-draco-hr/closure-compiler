{
  if (isUnknown()) {
    return UNKNOWN;
  }
  Preconditions.checkState(objs != null || enums != null);
  return nullAcceptingJoin(TypeWithProperties.getDeclaredProp(objs,qname),TypeWithProperties.getDeclaredProp(enums,qname));
}
