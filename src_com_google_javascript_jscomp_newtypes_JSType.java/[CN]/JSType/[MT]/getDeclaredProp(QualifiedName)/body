{
  if (isUnknown()) {
    return UNKNOWN;
  }
  Preconditions.checkState(getObjs() != null || getEnums() != null);
  return nullAcceptingJoin(TypeWithProperties.getDeclaredProp(getObjs(),qname),TypeWithProperties.getDeclaredProp(getEnums(),qname));
}
