{
  Preconditions.checkArgument(lhs.isSubtypeOf(TypeConsts.TOP_SCALAR) || rhs.isSubtypeOf(TypeConsts.TOP_SCALAR));
  if (lhs.isBottom() || rhs.isBottom() || lhs.isUnknown()|| rhs.isUnknown()|| (lhs.isBoolean() && rhs.isBoolean())|| lhs.equals(rhs)) {
    return true;
  }
  return false;
}
