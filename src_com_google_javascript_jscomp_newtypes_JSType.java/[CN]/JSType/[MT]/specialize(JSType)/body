{
  if (other.isTop() || other.isUnknown()) {
    return this;
  }
 else   if (other.isTruthy()) {
    return makeTruthy();
  }
 else   if (other.isFalsy()) {
    return makeFalsy();
  }
 else   if (this.isTop() || this.isUnknown()) {
    return other.withLocation(this.location);
  }
  String newTypevar=null;
  if (typeVar != null && other.typeVar != null && typeVar.equals(other.typeVar)) {
    newTypevar=typeVar;
  }
  return new JSType(this.type & other.type,this.location,ObjectType.specializeSet(this.objs,other.objs),newTypevar);
}
