{
  if (isBottom()) {
    return false;
  }
 else   if (getObjs() == null) {
    return true;
  }
  for (  ObjectType obj : getObjs()) {
    if (!obj.isInhabitable()) {
      return false;
    }
  }
  return true;
}
