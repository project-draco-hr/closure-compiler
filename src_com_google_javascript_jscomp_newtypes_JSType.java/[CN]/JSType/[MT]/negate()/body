{
  if (isTop() || isUnknown() || objs != null || typeVar != null) {
    return this;
  }
  if (isTruthy()) {
    return FALSY;
  }
 else   if (isFalsy()) {
    return TRUTHY;
  }
  return new JSType(TOP_SCALAR_MASK & ~mask);
}
