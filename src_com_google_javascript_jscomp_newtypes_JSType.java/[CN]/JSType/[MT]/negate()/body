{
  if (isTop() || isUnknown()) {
    return this;
  }
  if (isTruthy()) {
    return FALSY;
  }
 else   if (isFalsy()) {
    return TRUTHY;
  }
 else   if (objs == null && typeVar == null) {
    return new JSType(TOP_SCALAR_MASK & ~mask);
  }
  return UNKNOWN;
}
