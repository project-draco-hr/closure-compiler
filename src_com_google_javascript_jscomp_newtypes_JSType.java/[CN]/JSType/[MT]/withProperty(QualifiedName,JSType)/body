{
  Preconditions.checkArgument(type != null);
  if (isUnknown() || isBottom() || getObjs() == null) {
    return this;
  }
  return makeType(getMask(),ObjectType.withProperty(getObjs(),qname,type),getTypeVar(),getEnums());
}
