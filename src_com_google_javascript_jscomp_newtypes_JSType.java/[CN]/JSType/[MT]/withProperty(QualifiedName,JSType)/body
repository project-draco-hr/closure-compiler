{
  Preconditions.checkArgument(type != null);
  if (isUnknown()) {
    return this;
  }
  Preconditions.checkState(getObjs() != null);
  return new UnionType(getMask(),getLocation(),ObjectType.withProperty(getObjs(),qname,type),getTypeVar(),getEnums());
}
