{
  Preconditions.checkArgument(type != null);
  if (isUnknown()) {
    return this;
  }
  Preconditions.checkState(getObjs() != null);
  return makeType(getMask(),getLocation(),ObjectType.withProperty(getObjs(),qname,type),getTypeVar(),getEnums());
}
