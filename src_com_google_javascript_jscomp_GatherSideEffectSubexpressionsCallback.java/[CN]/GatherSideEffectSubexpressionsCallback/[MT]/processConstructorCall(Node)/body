{
  Preconditions.checkArgument(node.getType() == Token.NEW,"Expected: NEW, Got: %s",Token.name(node.getType()));
  if (!NodeUtil.constructorCallHasSideEffects(node)) {
    return true;
  }
  accumulator.keepSubTree(node);
  return false;
}
