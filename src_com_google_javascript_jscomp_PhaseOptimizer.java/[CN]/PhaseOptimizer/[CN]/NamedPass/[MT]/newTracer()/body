{
  String comment=name + (recentChange.hasCodeChanged() ? " on recently changed AST" : "");
  if (tracker != null) {
    tracker.recordPassStart(name,factory.isOneTimePass());
  }
  return new Tracer("JSCompiler",comment);
}
