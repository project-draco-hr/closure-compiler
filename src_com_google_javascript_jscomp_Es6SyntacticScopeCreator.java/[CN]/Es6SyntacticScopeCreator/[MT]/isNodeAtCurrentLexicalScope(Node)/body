{
  Node parent=n.getParent();
  Preconditions.checkState(parent.isBlock() || parent.isFor() || parent.isForOf()|| parent.isScript());
  return parent == scope.getRootNode() || parent.isScript() || (parent.getParent().isCatch() && parent.getParent().getParent() == scope.getRootNode());
}
