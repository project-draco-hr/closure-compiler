{
  String externs=LINE_JOINER.join("f().name;","/**"," * @param {string} s id."," * @return {string}"," * @nosideeffects"," */","f().name;");
  checkDefinitionsInExterns(externs,ImmutableSet.<String>of());
}
