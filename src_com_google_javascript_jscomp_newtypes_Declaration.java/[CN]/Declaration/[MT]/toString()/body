{
  return MoreObjects.toStringHelper(this).omitNullValues().add("simpleType",simpleType).add("typedef",typedef).add("namespace",namespaceLit).add("enum",enumType).add("scope",functionScope).add("nominal",nominal).toString();
}
