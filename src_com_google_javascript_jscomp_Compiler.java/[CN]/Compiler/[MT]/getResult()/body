{
  PassConfig.State state=getPassConfig().getIntermediateState();
  return new Result(getErrors(),getWarnings(),debugLog_.toString(),state.variableMap,state.propertyMap,state.anonymousFunctionNameMap,functionInformationMap_,sourceMap_,externExports_,state.cssNames);
}
