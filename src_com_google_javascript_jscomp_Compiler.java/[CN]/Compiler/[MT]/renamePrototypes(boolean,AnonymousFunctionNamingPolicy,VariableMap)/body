{
  logger_.info("Renaming prototypes");
  startPass("renamePrototypes");
  RenamePrototypes rp=new RenamePrototypes(this,aggressive,anonFunctionNamePolicy.getReservedCharacters(),prevPropertyMap);
  process(rp);
  propertyMap_=rp.getPropertyMap();
  endPass();
}
