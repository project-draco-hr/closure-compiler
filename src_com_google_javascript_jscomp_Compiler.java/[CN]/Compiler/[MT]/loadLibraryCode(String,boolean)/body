{
  String originalCode;
  try {
    originalCode=CharStreams.toString(new InputStreamReader(Compiler.class.getResourceAsStream(String.format("js/%s.js",resourceName)),UTF_8));
  }
 catch (  IOException e) {
    throw new RuntimeException(e);
  }
  Node ast=parseSyntheticCode(originalCode);
  if (normalizeAndUniquifyNames) {
    Normalize.normalizeSyntheticCode(this,ast,String.format("jscomp_%s_",resourceName));
  }
  return ast;
}
