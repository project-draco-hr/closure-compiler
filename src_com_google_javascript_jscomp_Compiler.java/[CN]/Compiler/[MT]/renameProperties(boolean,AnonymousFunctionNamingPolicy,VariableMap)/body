{
  logger_.info("Renaming properties");
  startPass("renameProperties");
  RenameProperties rp=new RenameProperties(this,generatePseudoNames,prevPropertyMap,anonFunctionNamePolicy.getReservedCharacters());
  process(rp);
  propertyMap_=rp.getPropertyMap();
  endPass();
}
