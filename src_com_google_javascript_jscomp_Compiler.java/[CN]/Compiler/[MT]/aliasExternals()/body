{
  logger_.info("Creating alias functions for externals");
  startPass("aliasExternals");
  AliasExternals ae=new AliasExternals(this,getModuleGraph(),options_.unaliasableGlobals,options_.aliasableGlobals);
  process(ae);
  endPass();
}
