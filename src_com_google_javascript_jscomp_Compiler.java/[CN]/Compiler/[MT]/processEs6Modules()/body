{
  for (  CompilerInput input : inputs) {
    input.setCompiler(this);
    Node root=input.getAstRoot(this);
    if (root == null) {
      continue;
    }
    new ProcessEs6Modules(this,ES6ModuleLoader.createNaiveLoader(this,options.commonJSModulePathPrefix),true).processFile(root);
  }
}
