{
  for (  String ctorName : ctors.keySet()) {
    if (!provides.containsKey(ctorName)) {
      compiler.report(JSError.make(t,ctors.get(ctorName),checkLevel,MISSING_PROVIDE_WARNING,ctorName));
    }
  }
  provides.clear();
  ctors.clear();
}
