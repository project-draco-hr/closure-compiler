{
  return new CompilerPass(){
    public void process(    Node externs,    Node root){
      new CollapseProperties(compiler,false,true).process(externs,root);
      new Normalize(compiler,false).new RenameConstants().process(externs,root);
    }
  }
;
}
