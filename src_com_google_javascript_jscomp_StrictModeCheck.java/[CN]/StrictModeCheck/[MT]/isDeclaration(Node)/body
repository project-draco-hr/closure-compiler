{
switch (n.getParent().getType()) {
case Token.LET:
case Token.CONST:
case Token.VAR:
case Token.FUNCTION:
case Token.CATCH:
    return true;
case Token.PARAM_LIST:
  return n.getGrandparent().isFunction();
default :
return false;
}
}
