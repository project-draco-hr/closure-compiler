{
  if (catchCond == null) {
    catchCond=new Node(Token.EMPTY,nameLineno,nameCharno);
  }
  return new Node(Token.CATCH,createName(varName,nameLineno,nameCharno),catchCond,stmts,catchLineno,catchCharno);
}
