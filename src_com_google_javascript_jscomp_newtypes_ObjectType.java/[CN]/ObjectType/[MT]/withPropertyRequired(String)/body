{
  Property oldProp=this.props.get(pname);
  Property newProp=oldProp == null ? UNKNOWN_PROP : Property.make(oldProp.getType(),oldProp.getDeclaredType());
  return makeObjectType(this.nominalType,this.props.with(pname,newProp),this.fn,this.ns,this.isLoose,this.objectKind);
}
