{
  if (concreteTypes.isEmpty() || !hasFreeTypeVars(new HashSet<String>())) {
    return this;
  }
  PersistentMap<String,Property> newProps=PersistentMap.create();
  for (  String p : props.keySet()) {
    newProps=newProps.with(p,props.get(p).substituteGenerics(concreteTypes));
  }
  return new ObjectType(nominalType == null ? null : nominalType.instantiateGenerics(concreteTypes),newProps,fn == null ? null : fn.substituteGenerics(concreteTypes),isLoose,objectKind);
}
