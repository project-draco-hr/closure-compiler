{
  if (nominalType != null && nominalType.hasFreeTypeVars(boundTypeVars) || fn != null && fn.hasFreeTypeVars(boundTypeVars)) {
    return true;
  }
  for (  Property prop : props.values()) {
    if (prop.hasFreeTypeVars(boundTypeVars)) {
      return true;
    }
  }
  return false;
}
