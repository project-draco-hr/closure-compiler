{
  Preconditions.checkState(areRelatedClasses(this.nominalType,obj2.nominalType));
  NominalType resultNominalType=NominalType.pickSubclass(this.nominalType,obj2.nominalType);
  return ObjectType.makeObjectType(resultNominalType,meetPropsHelper(true,resultNominalType,this.props,obj2.props),(fn == null) ? null : fn.specialize(obj2.fn),this.isLoose || obj2.isLoose);
}
