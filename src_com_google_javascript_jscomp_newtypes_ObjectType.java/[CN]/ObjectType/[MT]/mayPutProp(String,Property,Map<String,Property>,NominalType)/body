{
  Property nomProp=nom == null ? null : nom.getProp(pname);
  JSType propType=prop.getType();
  if (nomProp == null || (!propType.isUnknown() && propType.isSubtypeOf(nomProp.getType()))) {
    props.put(pname,prop);
  }
}
