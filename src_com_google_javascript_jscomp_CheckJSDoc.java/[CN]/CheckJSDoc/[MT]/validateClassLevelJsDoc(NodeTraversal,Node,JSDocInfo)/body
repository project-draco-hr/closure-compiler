{
  if (info != null && n.isMemberFunctionDef()) {
    if (info.isConstructor() || info.isInterface() || info.hasBaseType()|| info.getImplementedInterfaceCount() != 0 || info.getExtendedInterfacesCount() != 0) {
      t.report(n,DISALLOWED_MEMBER_JSDOC);
    }
  }
}
