{
  ParameterizedType arrOfNumber=createParameterizedType(ARRAY_TYPE,NUMBER_TYPE);
  ParameterizedType objType=createParameterizedType(OBJECT_TYPE,UNKNOWN_TYPE);
  assertTrue(arrOfNumber.isSubtype(objType));
  assertFalse(objType.isSubtype(arrOfNumber));
}
