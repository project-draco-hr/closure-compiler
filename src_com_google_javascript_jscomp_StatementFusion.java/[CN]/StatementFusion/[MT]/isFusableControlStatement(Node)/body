{
switch (n.getType()) {
case Token.IF:
case Token.THROW:
case Token.SWITCH:
case Token.EXPR_RESULT:
    return true;
case Token.RETURN:
  return n.hasChildren();
case Token.FOR:
return NodeUtil.isForIn(n) && !mayHaveSideEffects(n.getFirstChild());
}
return false;
}
