{
  ObjectType foo=ObjectType.fromNominalType(NominalType.fromRaw(RawNominalType.makeClass("Foo",null).finalizeNominalType()));
  ObjectType bar=ObjectType.fromNominalType(NominalType.fromRaw(RawNominalType.makeClass("Bar",null).finalizeNominalType()));
  assertTrue(foo.isSubtypeOf(foo));
  assertFalse(foo.isSubtypeOf(bar));
}
