{
  ObjectType foo=ObjectType.fromNominalType(NominalType.makeClass("Foo",null,0));
  ObjectType bar=ObjectType.fromNominalType(NominalType.makeClass("Bar",null,1));
  assertTrue(foo.isSubtypeOf(foo));
  assertFalse(foo.isSubtypeOf(bar));
}
