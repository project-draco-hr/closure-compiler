{
  ObjectType foo=ObjectType.fromNominalType(NominalType.fromRaw(RawNominalType.makeClass("Foo",null,0).finalizeNominalType()));
  ObjectType bar=ObjectType.fromNominalType(NominalType.fromRaw(RawNominalType.makeClass("Bar",null,1).finalizeNominalType()));
  assertTrue(foo.isSubtypeOf(foo));
  assertFalse(foo.isSubtypeOf(bar));
}
