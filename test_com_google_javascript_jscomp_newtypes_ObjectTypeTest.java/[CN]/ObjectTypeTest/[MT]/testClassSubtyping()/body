{
  ObjectType foo=ObjectType.fromNominalType(NominalType.makeClass("Foo",null));
  ObjectType bar=ObjectType.fromNominalType(NominalType.makeClass("Bar",null));
  assertTrue(foo.isSubtypeOf(foo));
  assertFalse(foo.isSubtypeOf(bar));
}
