{
  ObjectType foo=ObjectType.fromNominalType(NominalType.makeClass("Foo",null,0).finalizeNominalType());
  ObjectType bar=ObjectType.fromNominalType(NominalType.makeClass("Bar",null,1).finalizeNominalType());
  assertTrue(foo.isSubtypeOf(foo));
  assertFalse(foo.isSubtypeOf(bar));
}
