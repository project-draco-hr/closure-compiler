{
  for (  DefinitionSite defSite : definitions.getDefinitionSites().toArray(new DefinitionSite[0])) {
    if (definitions.getDefinitionSites().contains(defSite)) {
      optimizeResultsIfEligible(defSite,definitions);
    }
  }
}
