{
  super.visit(t,n,parent);
switch (n.getType()) {
case Token.ASSIGN:
    checkForOldStyleTypedef(t,n);
  break;
case Token.VAR:
if (n.hasOneChild()) {
  checkForOldStyleTypedef(t,n);
  checkForTypedef(t,n.getFirstChild(),n.getJSDocInfo());
}
break;
}
}
