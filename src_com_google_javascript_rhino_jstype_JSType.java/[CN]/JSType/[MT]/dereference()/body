{
  JSType restricted=restrictByNotNullOrUndefined();
  JSType autobox=restricted.autoboxesTo();
  JSType result=autobox == null ? restricted : autobox;
  return result instanceof ObjectType ? (ObjectType)result : null;
}
