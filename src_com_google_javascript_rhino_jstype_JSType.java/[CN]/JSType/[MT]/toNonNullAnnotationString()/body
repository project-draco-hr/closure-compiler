{
  if (isEnumElementType()) {
    return toMaybeEnumElementType().getReferenceName();
  }
  return !isUnknownType() && !isTemplateType() && !isRecordType()&& !isFunctionType()&& isObject() ? "!" + toAnnotationString() : toAnnotationString();
}
