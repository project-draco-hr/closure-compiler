{
  Preconditions.checkState(parent != null);
switch (parent.getType()) {
case SCRIPT:
case MODULE_BODY:
case BLOCK:
case LABEL:
case NAMESPACE_ELEMENTS:
    return true;
default :
  return false;
}
}
