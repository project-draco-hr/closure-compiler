{
  Preconditions.checkState(parent != null);
switch (parent.getToken()) {
case SCRIPT:
case MODULE_BODY:
case BLOCK:
case LABEL:
case NAMESPACE_ELEMENTS:
    return true;
default :
  return false;
}
}
