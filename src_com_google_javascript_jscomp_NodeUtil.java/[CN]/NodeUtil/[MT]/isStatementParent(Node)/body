{
  Preconditions.checkState(parent != null);
switch (parent.getType()) {
case Token.SCRIPT:
case Token.BLOCK:
case Token.LABEL:
case Token.NAMESPACE_ELEMENTS:
    return true;
default :
  return false;
}
}
