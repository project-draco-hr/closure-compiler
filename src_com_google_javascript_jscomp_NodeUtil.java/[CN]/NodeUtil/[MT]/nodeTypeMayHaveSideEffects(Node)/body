{
  if (isAssignmentOp(n)) {
    return true;
  }
switch (n.getType()) {
case Token.CALL:
case Token.DELPROP:
case Token.NEW:
case Token.DEC:
case Token.INC:
case Token.THROW:
    return true;
case Token.NAME:
  return n.hasChildren();
default :
return false;
}
}
