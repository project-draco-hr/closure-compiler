{
  Preconditions.checkArgument(n.isQualifiedName());
switch (n.getParent().getType()) {
case Token.ASSIGN:
    return n.getNext();
case Token.VAR:
  return n.getFirstChild();
default :
return null;
}
}
