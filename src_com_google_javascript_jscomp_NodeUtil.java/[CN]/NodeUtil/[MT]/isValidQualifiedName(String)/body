{
  if (name.endsWith(".") || name.startsWith(".")) {
    return false;
  }
  for (  String part : Splitter.on('.').split(name)) {
    if (!isValidSimpleName(part)) {
      return false;
    }
  }
  return true;
}
