{
switch (n.getType()) {
case Token.BLOCK:
    if (n.getParent() == null || n.getParent().getParent() == null || n.getParent().isCatch()) {
      return false;
    }
  return true;
case Token.FOR:
case Token.FOR_OF:
return true;
}
return false;
}
