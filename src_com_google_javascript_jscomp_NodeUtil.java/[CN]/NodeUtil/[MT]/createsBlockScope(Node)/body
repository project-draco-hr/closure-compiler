{
switch (n.getType()) {
case Token.BLOCK:
    if (n.getParent() == null || n.getParent().isCatch() || n.getFirstChild() != null && n.getFirstChild().isScript()) {
      return false;
    }
  return true;
case Token.FOR:
case Token.FOR_OF:
return true;
}
return false;
}
