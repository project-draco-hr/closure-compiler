{
switch (n.getType()) {
case Token.FUNCTION:
case Token.SCRIPT:
    return true;
case Token.BLOCK:
  if (n.getParent() == null || n.getFirstChild() != null && n.getFirstChild().isScript()) {
    return true;
  }
default :
return false;
}
}
