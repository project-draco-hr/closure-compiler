{
switch (n.getType()) {
case Token.FUNCTION:
case Token.SCRIPT:
    return true;
case Token.BLOCK:
  if (n.getParent() == null || n.getParent().getParent() == null) {
    return true;
  }
default :
return false;
}
}
