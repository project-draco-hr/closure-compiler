{
  mode=LanguageMode.ECMASCRIPT5;
  expectedFeatures=FeatureSet.ES6;
  parseWarning("[x,y] = foo();","this language feature is only supported in es6 mode: destructuring");
  mode=LanguageMode.ECMASCRIPT6;
  parse("[x,y] = foo();");
  parse("[x,y[15]] = foo();");
}
