{
  mode=LanguageMode.ECMASCRIPT3;
  parseError("var x = {set a(x){}};","setters are not supported in Internet Explorer");
  mode=LanguageMode.ECMASCRIPT5;
  parse("var x = {set a(x){}};");
  parseError("var x = {set a(){}};","setters must have exactly one parameter");
}
