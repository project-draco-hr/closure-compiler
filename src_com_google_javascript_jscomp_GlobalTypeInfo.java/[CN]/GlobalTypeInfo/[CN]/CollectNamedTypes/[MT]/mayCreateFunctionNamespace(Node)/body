{
  if (!qnameNode.isName()) {
    return false;
  }
  Preconditions.checkState(!currentScope.isNamespace(qnameNode));
  if (currentScope.isKnownFunction(qnameNode.getString())) {
    visitNamespaceHelper(qnameNode,false);
    return true;
  }
  return false;
}
