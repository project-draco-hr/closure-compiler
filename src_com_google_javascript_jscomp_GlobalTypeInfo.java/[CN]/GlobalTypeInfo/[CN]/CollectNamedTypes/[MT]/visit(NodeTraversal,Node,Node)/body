{
switch (n.getType()) {
case Token.FUNCTION:
    initFnScope(n,currentScope);
  break;
case Token.VAR:
if (NodeUtil.isTypedefDecl(n)) {
  visitTypedef(n);
}
 else if (NodeUtil.isEnumDecl(n)) {
  visitEnum(n);
}
break;
case Token.GETPROP:
break;
}
}
