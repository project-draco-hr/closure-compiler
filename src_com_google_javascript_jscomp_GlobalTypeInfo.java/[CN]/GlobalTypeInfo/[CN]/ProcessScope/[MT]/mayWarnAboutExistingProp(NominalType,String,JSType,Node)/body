{
  if (classType.mayHaveOwnProp(pname) && classType.getPropDeclaredType(pname) != null) {
    warnings.add(JSError.make(propCreationNode,DUPLICATE_PROPERTY_JSDOC,pname,classType.toString()));
    return true;
  }
  return false;
}
