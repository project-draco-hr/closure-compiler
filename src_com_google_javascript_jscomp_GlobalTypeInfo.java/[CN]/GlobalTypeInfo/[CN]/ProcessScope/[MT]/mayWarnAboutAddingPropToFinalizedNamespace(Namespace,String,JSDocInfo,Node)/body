{
  if (ns.isNamespaceFinalized()) {
    if (!ns.isDefined(new QualifiedName(pname)) || jsdoc != null) {
      warnings.add(JSError.make(declNode,NAMESPACE_MODIFIED_AFTER_FINALIZATION,ns.getName(),ns.getConstDeclNode().toString()));
    }
    return true;
  }
  return false;
}
