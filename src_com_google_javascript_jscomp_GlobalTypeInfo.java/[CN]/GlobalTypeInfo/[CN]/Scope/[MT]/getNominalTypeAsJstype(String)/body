{
  Preconditions.checkState(localClassDefs != null);
  NominalType klass=getNominalType(name);
  if (klass != null) {
    return JSType.join(JSType.fromObjectType(ObjectType.fromClass(klass)),TypeConsts.NULL);
  }
  return null;
}
