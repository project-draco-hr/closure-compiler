{
  Iterator<String> it=localFunDefs.keySet().iterator();
  while (it.hasNext()) {
    String name=it.next();
    if (!TypeUtils.isIdentifier(name)) {
      it.remove();
    }
  }
  localNamespaces=null;
  localClassDefs=null;
}
