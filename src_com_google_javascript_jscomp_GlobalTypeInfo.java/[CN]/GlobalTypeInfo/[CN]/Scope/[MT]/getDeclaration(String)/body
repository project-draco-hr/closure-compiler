{
  Preconditions.checkArgument(!name.contains("."));
  Declaration decl=getLocalDeclaration(name);
  if (decl != null) {
    return decl;
  }
  return parent == null ? null : parent.getDeclaration(name);
}
