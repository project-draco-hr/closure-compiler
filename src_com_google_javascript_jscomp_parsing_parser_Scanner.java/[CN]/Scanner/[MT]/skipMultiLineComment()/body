{
  nextChar();
  nextChar();
  while (!isAtEnd() && (peekChar() != '*' || peekChar(1) != '/')) {
    nextChar();
  }
  if (isAtEnd()) {
    reportError("unterminated comment");
  }
  nextChar();
  nextChar();
}
