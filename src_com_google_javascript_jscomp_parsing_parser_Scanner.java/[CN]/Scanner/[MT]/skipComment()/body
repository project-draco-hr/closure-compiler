{
  skipWhitespace();
  if (!isAtEnd() && peek('/')) {
switch (peekChar(1)) {
case '/':
      skipSingleLineComment();
    return true;
case '*':
  skipMultiLineComment();
return true;
}
}
return false;
}
