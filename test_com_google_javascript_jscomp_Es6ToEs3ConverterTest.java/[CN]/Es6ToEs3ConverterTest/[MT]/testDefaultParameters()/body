{
  enableTypeCheck(CheckLevel.WARNING);
  test(Joiner.on('\n').join("var x = true;","function f(a=x) { var x = false; return a; }"),Joiner.on('\n').join("var x = true;","function f(a) {","  a = (a === undefined) ? x : a;","  var x$0 = false;","  return a;","}"));
  test("function f(zero, one = 1, two = 2) {}; f(1); f(1,2,3);",Joiner.on('\n').join("function f(zero, one, two) {","  one = (one === undefined) ? 1 : one;","  two = (two === undefined) ? 2 : two;","};","f(1); f(1,2,3);"));
  test("function f(zero, one = 1, two = 2) {}; f();",Joiner.on('\n').join("function f(zero, one, two) {","  one = (one === undefined) ? 1 : one;","  two = (two === undefined) ? 2 : two;","}; f();"),null,TypeCheck.WRONG_ARGUMENT_COUNT);
}
