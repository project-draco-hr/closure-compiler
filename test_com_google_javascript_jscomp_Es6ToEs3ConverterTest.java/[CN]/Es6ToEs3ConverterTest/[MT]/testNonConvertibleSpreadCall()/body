{
  enableAstValidation(false);
  test("Factory.create().m(...arr);",Joiner.on('\n').join("var $jscomp$spread$args0 = Factory.create();","$jscomp$spread$args0.m.apply($jscomp$spread$args0, [].concat(arr));"),Es6ToEs3Converter.CANNOT_CONVERT_YET);
}
