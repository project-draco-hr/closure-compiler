{
  enableAstValidation(false);
  testError("var C = new (class {})();",Es6ToEs3Converter.CANNOT_CONVERT);
  testError("var C = new (foo || (foo = class { }))();",Es6ToEs3Converter.CANNOT_CONVERT);
  testError("(condition ? obj1 : obj2).prop = class C { };",Es6ToEs3Converter.CANNOT_CONVERT);
}
