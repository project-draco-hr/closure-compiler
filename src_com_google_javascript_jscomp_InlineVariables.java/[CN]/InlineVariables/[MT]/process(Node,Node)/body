{
  ReferenceCollectingCallback callback=new ReferenceCollectingCallback(compiler,new InliningBehavior(),onlyConstants ? identifyConstants : Predicates.<Var>or(identifyConstants,Predicates.<Var>alwaysTrue()));
  callback.process(externs,root);
}
