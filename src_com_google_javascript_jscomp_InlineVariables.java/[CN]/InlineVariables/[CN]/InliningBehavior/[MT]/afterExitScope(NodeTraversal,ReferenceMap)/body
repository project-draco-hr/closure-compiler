{
  if (NodeUtil.isFunctionBlock(t.getScopeRoot()) || t.inGlobalScope() || NodeUtil.isTryCatchNodeContainer(t.getScopeRoot())) {
    collectAliasCandidates(t,referenceMap);
    doInlinesForScope(t,referenceMap);
  }
}
