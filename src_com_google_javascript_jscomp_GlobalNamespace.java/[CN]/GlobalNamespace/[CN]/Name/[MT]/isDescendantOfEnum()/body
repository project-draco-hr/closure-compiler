{
  if (parent == null) {
    return false;
  }
  if (parent.type == Type.OBJECTLIT && parent.docInfo != null && parent.docInfo.hasEnumParameterType()) {
    return true;
  }
  return parent.isDescendantOfEnum();
}
