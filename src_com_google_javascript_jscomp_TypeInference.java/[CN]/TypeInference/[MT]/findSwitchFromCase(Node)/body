{
  for (  DiGraphNode<Node,Branch> gn : getCfg().getDirectedPredNodes(caseNode)) {
    Node n=gn.getValue();
switch (n.getType()) {
case Token.SWITCH:
      return n;
case Token.CASE:
    return findSwitchFromCase(n);
}
}
throw new IllegalArgumentException("ill-formed case node");
}
