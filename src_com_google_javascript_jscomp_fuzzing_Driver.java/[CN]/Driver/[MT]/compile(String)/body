{
  Compiler compiler=new Compiler();
  compiler.compile(Arrays.asList(SourceFile.fromCode("[externs]","")),Arrays.asList(SourceFile.fromCode("[fuzzedCode]",code)),getOptions());
  return compiler;
}
