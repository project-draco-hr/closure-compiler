{
  if (config == null) {
    File file=new File(configFileName);
    try {
      config=new Gson().fromJson(Files.toString(file,StandardCharsets.UTF_8),JsonObject.class);
    }
 catch (    JsonParseException|IOException e) {
      e.printStackTrace();
    }
  }
  return config;
}
