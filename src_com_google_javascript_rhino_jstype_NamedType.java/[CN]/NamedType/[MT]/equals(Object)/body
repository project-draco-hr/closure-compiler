{
  if (this == that) {
    return true;
  }
 else   if (that instanceof ObjectType) {
    ObjectType objType=(ObjectType)that;
    return objType.isNominalType() && reference.equals(objType.getName());
  }
  return false;
}
