{
  ExistingIdentifierFuzzer idFuzzer=context.strict ? new ExistingIdentifierFuzzer(context,Type.FUNCTION,false) : new ExistingIdentifierFuzzer(context);
  candidates=new AbstractFuzzer[]{idFuzzer,new GetPropFuzzer(context),new GetElemFuzzer(context),new FunctionFuzzer(context,true)};
}
