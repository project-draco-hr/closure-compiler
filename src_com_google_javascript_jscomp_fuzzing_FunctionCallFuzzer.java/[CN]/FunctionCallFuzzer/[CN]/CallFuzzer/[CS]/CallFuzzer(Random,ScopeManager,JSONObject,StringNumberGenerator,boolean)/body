{
  super(random,scopeManager,config,snGenerator);
  if (isConstructor) {
    nodeType=Token.NEW;
    configName="constructorCall";
  }
 else {
    nodeType=Token.CALL;
    configName="normalCall";
  }
}
