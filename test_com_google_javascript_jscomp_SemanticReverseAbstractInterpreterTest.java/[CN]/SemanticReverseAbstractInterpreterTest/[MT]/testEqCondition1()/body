{
  FlowScope blind=newScope();
  testBinop(blind,Token.EQ,createVar(blind,"a",createUnionType(BOOLEAN_TYPE,VOID_TYPE)),createNull(),Sets.newHashSet(new Pair<String,JSType>("a",VOID_TYPE)),Sets.newHashSet(new Pair<String,JSType>("a",BOOLEAN_TYPE)));
}
