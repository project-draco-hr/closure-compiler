{
  FlowScope blind=newScope();
  testBinop(blind,Token.SHNE,createNumber(56),createVar(blind,"a",createUnionType(STRING_TYPE,NUMBER_TYPE)),Sets.newHashSet(new Pair<String,JSType>("a",createUnionType(STRING_TYPE,NUMBER_TYPE))),Sets.newHashSet(new Pair<String,JSType>("a",NUMBER_TYPE)));
}
