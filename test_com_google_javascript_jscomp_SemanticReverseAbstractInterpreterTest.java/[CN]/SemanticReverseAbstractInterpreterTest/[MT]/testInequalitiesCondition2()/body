{
  for (  int op : Arrays.asList(Token.LT,Token.GT,Token.LE,Token.GE)) {
    FlowScope blind=newScope();
    testBinop(blind,op,createVar(blind,"a",createUnionType(STRING_TYPE,NUMBER_TYPE,VOID_TYPE)),createVar(blind,"b",createUnionType(NUMBER_TYPE,NULL_TYPE)),Sets.newHashSet(new Pair<String,JSType>("a",createUnionType(STRING_TYPE,NUMBER_TYPE)),new Pair<String,JSType>("b",createUnionType(NUMBER_TYPE,NULL_TYPE))),Sets.newHashSet(new Pair<String,JSType>("a",createUnionType(STRING_TYPE,NUMBER_TYPE,VOID_TYPE)),new Pair<String,JSType>("b",createUnionType(NUMBER_TYPE,NULL_TYPE))));
  }
}
