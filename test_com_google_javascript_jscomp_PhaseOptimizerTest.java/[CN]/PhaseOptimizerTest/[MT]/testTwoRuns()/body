{
  addOneTimePass("x");
  optimizer.process(null,null);
  try {
    optimizer.process(null,null);
    fail();
  }
 catch (  IllegalStateException e) {
    assertEquals("One-time passes cannot be run multiple times: x",e.getMessage());
  }
}
