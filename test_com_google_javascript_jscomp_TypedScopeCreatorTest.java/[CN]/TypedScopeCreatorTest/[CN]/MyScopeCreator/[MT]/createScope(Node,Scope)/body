{
  Scope typedScope=delegate.createScope(root,parent);
  TypeInference typeInference=new TypeInference(compiler,computeCfg(root),compiler.getReverseAbstractInterpreter(),typedScope);
  typeInference.analyze();
  return typedScope;
}
