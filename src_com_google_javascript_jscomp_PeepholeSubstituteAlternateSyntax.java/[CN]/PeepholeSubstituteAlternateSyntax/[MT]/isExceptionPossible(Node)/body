{
  Preconditions.checkState(n.getType() == Token.RETURN || n.getType() == Token.THROW);
  return n.getType() == Token.THROW || (n.hasChildren() && !NodeUtil.isLiteralValue(n.getLastChild(),true));
}
