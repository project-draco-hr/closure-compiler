{
  int result=0;
  if (node.isNot()) {
    result++;
  }
  for (Node n=node.getFirstChild(); n != null; n=n.getNext()) {
    if (NodeUtil.precedenceWithDefault(n.getType()) < NodeUtil.precedenceWithDefault(node.getType())) {
      result+=2;
    }
    result+=length(n);
  }
  return result;
}
