{
  for (Node c=n.getFirstChild(); c != null; c=c.getNext()) {
switch (c.getType()) {
case Token.FUNCTION:
case Token.AND:
case Token.OR:
case Token.HOOK:
      return true;
  }
  if (mayHaveConditionalCode(c)) {
    return true;
  }
}
return false;
}
