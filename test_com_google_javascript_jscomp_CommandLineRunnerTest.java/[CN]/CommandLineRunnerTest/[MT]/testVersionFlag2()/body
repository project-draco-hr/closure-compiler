{
  lastArg="--version";
  assertFalse(createCommandLineRunner(new String[]{"function f() {}"}).shouldRunCompiler());
  assertEquals(0,new String(outReader.toByteArray(),UTF_8).indexOf("Closure Compiler (http://github.com/google/closure-compiler)\n" + "Version: "));
}
