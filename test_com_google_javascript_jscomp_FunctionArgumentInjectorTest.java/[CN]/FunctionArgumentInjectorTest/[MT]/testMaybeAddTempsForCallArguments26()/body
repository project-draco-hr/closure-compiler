{
  testNeededTemps("function foo(a){}; foo(new Bar());","foo",Sets.newHashSet("a"));
  testNeededTemps("function foo(a){a;}; foo(new Bar());","foo",Sets.newHashSet("a"));
  testNeededTemps("function foo(a){a;a;}; foo(new Bar());","foo",Sets.newHashSet("a"));
}
