{
  testNeededTemps("function foo(a){}; foo(new Date());","foo",EMPTY_STRING_SET);
  testNeededTemps("function foo(a){a;}; foo(new Date());","foo",Sets.newHashSet("a"));
  testNeededTemps("function foo(a){a;a;}; foo(new Date());","foo",Sets.newHashSet("a"));
}
