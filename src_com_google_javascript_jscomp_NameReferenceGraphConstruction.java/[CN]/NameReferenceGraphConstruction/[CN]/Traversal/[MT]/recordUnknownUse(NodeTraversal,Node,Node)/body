{
  if (isExtern) {
    return;
  }
 else {
    Preconditions.checkArgument(NodeUtil.isGetProp(n));
    Reference ref=new Reference(n,parent);
    ref.setUnknown(true);
    unknownNameUse.put(n.getLastChild().getString(),new Pair<Name,Reference>(getNamedContainingFunction(),ref));
  }
}
