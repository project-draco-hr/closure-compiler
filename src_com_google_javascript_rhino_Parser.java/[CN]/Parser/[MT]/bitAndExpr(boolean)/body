{
  Node pn=eqExpr(inForInit);
  while (matchToken(Token.BITAND)) {
    int lineno=ts.getLineno();
    int charno=ts.getCharno();
    decompiler.addToken(Token.BITAND);
    pn=nf.createBinary(Token.BITAND,pn,eqExpr(inForInit),lineno,charno);
  }
  return pn;
}
