{
  Scope scope=null;
  for (  Reference ref : references) {
    if (scope == null) {
      scope=ref.scope;
    }
 else     if (scope != ref.scope.getClosestHoistScope()) {
      return true;
    }
  }
  return false;
}
