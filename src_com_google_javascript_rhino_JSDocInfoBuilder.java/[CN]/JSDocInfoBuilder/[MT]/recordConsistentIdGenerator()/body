{
  if (!currentInfo.isConsistentIdGenerator() && !currentInfo.isStableIdGenerator()) {
    currentInfo.setConsistentIdGenerator(true);
    populated=true;
    return true;
  }
 else {
    return false;
  }
}
