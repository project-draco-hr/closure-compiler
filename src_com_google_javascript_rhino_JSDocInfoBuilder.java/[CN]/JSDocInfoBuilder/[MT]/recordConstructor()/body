{
  if (!hasAnySingletonTypeTags() && !currentInfo.isConstructorOrInterface() && !currentInfo.isAbstract()) {
    currentInfo.setConstructor(true);
    populated=true;
    return true;
  }
 else {
    return false;
  }
}
