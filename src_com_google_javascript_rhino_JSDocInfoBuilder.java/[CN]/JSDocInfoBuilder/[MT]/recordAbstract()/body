{
  if (!hasAnySingletonTypeTags() && !currentInfo.isInterface() && !currentInfo.isConstructor()&& !currentInfo.isAbstract()) {
    currentInfo.setAbstract();
    populated=true;
    return true;
  }
  return false;
}
