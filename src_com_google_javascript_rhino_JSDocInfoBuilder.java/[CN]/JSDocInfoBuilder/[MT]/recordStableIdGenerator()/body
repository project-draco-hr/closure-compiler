{
  if (currentInfo.isStableIdGenerator() || currentInfo.isIdGenerator() || currentInfo.isConsistentIdGenerator()) {
    return false;
  }
 else {
    currentInfo.setStableIdGenerator(true);
    populated=true;
    return true;
  }
}
