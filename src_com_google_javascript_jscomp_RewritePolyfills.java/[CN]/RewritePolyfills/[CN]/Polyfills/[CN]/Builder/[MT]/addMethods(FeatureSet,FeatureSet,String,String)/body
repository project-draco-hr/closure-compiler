{
  if (!base.isEmpty()) {
    for (    String method : methods) {
      methodsBuilder.put(method,new Polyfill(nativeVersion,polyfillVersion,"",base + "." + method+ "$install();"));
    }
  }
  return this;
}
