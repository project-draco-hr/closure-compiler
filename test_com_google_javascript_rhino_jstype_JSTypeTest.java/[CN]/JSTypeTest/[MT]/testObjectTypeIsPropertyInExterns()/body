{
  ObjectType sup=registry.createObjectType(registry.createAnonymousObjectType());
  ObjectType sub=registry.createObjectType(sup);
  sup.defineProperty("externProp",null,false,true,null);
  sub.defineProperty("externProp",null,false,false,null);
  assertTrue(sup.isPropertyInExterns("externProp"));
  assertFalse(sub.isPropertyInExterns("externProp"));
}
