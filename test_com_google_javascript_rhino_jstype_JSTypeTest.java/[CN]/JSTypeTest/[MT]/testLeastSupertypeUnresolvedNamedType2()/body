{
  JSType expected=registry.createUnionType(unresolvedNamedType,UNKNOWN_TYPE);
  assertEquals(expected,unresolvedNamedType.getLeastSupertype(UNKNOWN_TYPE));
  assertEquals(expected,UNKNOWN_TYPE.getLeastSupertype(unresolvedNamedType));
  assertEquals(UNKNOWN_TYPE,expected);
}
