{
  ObjectType anonymous=registry.createAnonymousObjectType();
  anonymous.defineDeclaredProperty("a",NUMBER_TYPE,false);
  anonymous.defineDeclaredProperty("b",NUMBER_TYPE,false);
  anonymous.defineDeclaredProperty("c",NUMBER_TYPE,false);
  anonymous.defineDeclaredProperty("d",NUMBER_TYPE,false);
  anonymous.defineDeclaredProperty("e",NUMBER_TYPE,false);
  anonymous.defineDeclaredProperty("f",NUMBER_TYPE,false);
  assertEquals("{a: number, b: number, c: number, d: number, ...}",anonymous.toString());
}
