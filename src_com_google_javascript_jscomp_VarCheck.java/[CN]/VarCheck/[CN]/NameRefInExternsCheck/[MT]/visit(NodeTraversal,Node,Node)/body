{
  if (n.getType() == Token.NAME) {
switch (parent.getType()) {
case Token.VAR:
case Token.FUNCTION:
case Token.GETPROP:
case Token.LP:
      break;
default :
    t.report(n,NAME_REFERENCE_IN_EXTERNS_ERROR,n.getString());
  break;
}
}
}
