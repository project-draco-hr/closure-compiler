{
  StringBuffer nl=new StringBuffer();
  for (int i=0; i < rv.length(); i++) {
    if (rv.charAt(i) == '\r') {
      if (rv.charAt(i + 1) == '\n') {
      }
 else {
        nl.append('\n');
      }
    }
 else {
      nl.append(rv.charAt(i));
    }
  }
  return nl.toString();
}
