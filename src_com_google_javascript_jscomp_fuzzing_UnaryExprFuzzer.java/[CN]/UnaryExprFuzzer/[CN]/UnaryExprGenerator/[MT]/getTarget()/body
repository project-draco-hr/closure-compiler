{
  if (target == null) {
    if (operator.hasSideEffect) {
      target=new AssignableExprFuzzer(random,scopeManager,config,snGenerator);
    }
 else {
      target=new ExpressionFuzzer(random,scopeManager,config,snGenerator);
    }
  }
  return target;
}
