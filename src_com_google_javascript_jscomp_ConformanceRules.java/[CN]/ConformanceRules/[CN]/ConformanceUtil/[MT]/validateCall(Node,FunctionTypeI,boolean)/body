{
  Preconditions.checkState(callOrNew.isCall() || callOrNew.isNew());
  return validateParameterList(callOrNew,functionType,isCallInvocation) && validateThis(callOrNew,functionType,isCallInvocation);
}
