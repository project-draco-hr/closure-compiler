{
  if (n.isGetProp() && isKnownThis(n.getFirstChild()) && isUnknown(n)&& !isTemplateType(n)&& isUsed(n)&& !isWhiteListed(n)) {
    return ConformanceResult.VIOLATION;
  }
  return ConformanceResult.CONFORMANCE;
}
