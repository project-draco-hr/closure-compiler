{
  Preconditions.checkState(enumPropType != null);
  PersistentMap<String,Property> propMap=PersistentMap.create();
  for (  String s : props) {
    propMap=propMap.with(s,Property.makeConstant(enumPropType,enumPropType));
  }
  return JSType.fromObjectType(ObjectType.makeObjectType(null,propMap,null,false,ObjectKind.UNRESTRICTED));
}
