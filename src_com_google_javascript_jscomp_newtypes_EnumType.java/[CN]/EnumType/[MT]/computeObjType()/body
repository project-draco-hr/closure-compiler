{
  Preconditions.checkState(enumPropType != null);
  PersistentMap<String,Property> propMap=otherProps;
  for (  String s : props) {
    propMap=propMap.with(s,Property.makeConstant(null,enumPropType,enumPropType));
  }
  return withNamedTypes(ObjectType.makeObjectType(null,propMap,null,false,ObjectKind.UNRESTRICTED));
}
