{
  test(Joiner.on('\n').join("const arr = [];","for (let i = 0; i < 10; i++) {","  arr.push(function() { return i; });","}"),null,Es6ToEs3Converter.CANNOT_CONVERT_YET);
  test(Joiner.on('\n').join("const arr = [];","for (let i = 0; i < 10; i++) {","  let y = i;","  arr.push(function() { return y; });","}"),null,Es6ToEs3Converter.CANNOT_CONVERT_YET);
  test(Joiner.on('\n').join("const arr = [];","while (true) {","  let i = 0;","  arr.push(function() { return i; });","}"),null,Es6ToEs3Converter.CANNOT_CONVERT_YET);
  test(Joiner.on('\n').join("const arr = [];","for (let i = 0; i < 10; i++) {","  arr.push(function() { return ++i; });","}"),null,Es6ToEs3Converter.CANNOT_CONVERT_YET);
}
