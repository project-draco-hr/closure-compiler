{
  Node ttlExp=buildUnionType(buildTypeVariable("T"),buildTypeVariable("R"));
  ImmutableMap<String,JSType> typeVars=ImmutableMap.of("T",NUMBER_TYPE,"R",STRING_TYPE);
  assertTypeEquals(createUnionType(NUMBER_TYPE,STRING_TYPE),transform(ttlExp,typeVars));
}
