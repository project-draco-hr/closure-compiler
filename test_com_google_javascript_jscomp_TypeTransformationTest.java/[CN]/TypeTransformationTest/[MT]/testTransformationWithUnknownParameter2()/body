{
  Node ttlExp=buildUnionType(buildTypeVariable("T"),buildTypePredicate("foo"));
  ImmutableMap<String,JSType> typeVars=ImmutableMap.of("T",STRING_TYPE);
  assertTypeEquals(UNKNOWN_TYPE,transform(ttlExp,typeVars));
}
