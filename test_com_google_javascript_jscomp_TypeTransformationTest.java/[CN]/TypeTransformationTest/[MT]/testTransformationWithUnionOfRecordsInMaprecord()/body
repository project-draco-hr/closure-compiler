{
  testTTL(UNKNOWN_TYPE,"maprecord(union(record({n:N}), record({s:S})), " + "(k, v) => record({[k]:v}))","The first parameter of a maprecord must be a record type, " + "found ({n: number}|{s: string})");
}
