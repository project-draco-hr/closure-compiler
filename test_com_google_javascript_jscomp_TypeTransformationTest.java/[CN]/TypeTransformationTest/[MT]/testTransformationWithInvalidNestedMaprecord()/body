{
  testTTL(UNKNOWN_TYPE,"maprecord(NESTEDREC, (k, v) => maprecord(v, (k, v) => BOT))","The body of a maprecord function must evaluate to a record type " + "or a no type, found ?","The variable k is already defined");
}
