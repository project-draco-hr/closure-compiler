{
  testTTL(record("foo",record("n",NUMBER_TYPE,"s",ARRAY_TYPE,"r",record("x",NUMBER_TYPE,"y",NUMBER_TYPE))),"maprecord(record({a:record({n:N, r:record({x:N})}), " + "b:record({s:ARR, r:record({y:N})})})," + "(k, v) => record({foo:v}))");
}
