{
  if (n1.isFunction()) {
    map.put(n1,n2);
  }
  for (Node child1=n1.getFirstChild(), child2=n2.getFirstChild(); child1 != null; child1=child1.getNext(), child2=child2.getNext()) {
    correspondingNodesHelper(map,child1,child2);
  }
}
