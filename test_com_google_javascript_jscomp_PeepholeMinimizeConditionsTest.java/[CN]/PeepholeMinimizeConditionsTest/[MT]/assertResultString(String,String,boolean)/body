{
  PeepholeMinimizeConditionsTest scTest=new PeepholeMinimizeConditionsTest(false);
  if (normalize) {
    scTest.enableNormalize();
  }
 else {
    scTest.disableNormalize();
  }
  scTest.test(js,expected);
}
