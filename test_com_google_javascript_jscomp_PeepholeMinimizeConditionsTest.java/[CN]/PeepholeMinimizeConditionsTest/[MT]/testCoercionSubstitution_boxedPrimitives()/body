{
  enableTypeCheck();
  testSame("var x = new Number();\nif (x) throw 'a';\n");
  testSame("var x = new String();\nif (x) throw 'a';\n");
  testSame("var x = new Boolean();\nif (x) throw 'a';\n");
}
