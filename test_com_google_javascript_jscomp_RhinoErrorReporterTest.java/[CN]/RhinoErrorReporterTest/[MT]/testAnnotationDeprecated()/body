{
  String message="The @expose annotation is deprecated. Use @nocollapse or @export " + "instead.";
  JSError error=assertWarning("/** @expose */ var x = 1;",RhinoErrorReporter.ANNOTATION_DEPRECATED,message);
  assertEquals(1,error.getLineNumber());
  assertEquals(15,error.getCharno());
}
