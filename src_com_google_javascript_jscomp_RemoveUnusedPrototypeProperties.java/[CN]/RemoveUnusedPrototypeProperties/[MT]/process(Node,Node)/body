{
  AnalyzePrototypeProperties analyzer=new AnalyzePrototypeProperties(compiler,null,canModifyExternsProtypeProps,anchorUnusedVars,canModifyThisProperties,anchorObjectLiteralProperties);
  analyzer.process(externRoot,root);
  removeUnusedSymbols(analyzer.getAllNameInfo());
}
