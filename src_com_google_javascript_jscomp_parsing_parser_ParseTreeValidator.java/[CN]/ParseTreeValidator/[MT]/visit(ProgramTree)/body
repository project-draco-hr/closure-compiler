{
  for (  ParseTree sourceElement : tree.sourceElements) {
    checkVisit(sourceElement.isSourceElement() || sourceElement.type == ParseTreeType.CLASS_DECLARATION || sourceElement.type == ParseTreeType.MODULE_DEFINITION,sourceElement,"global source element expected");
  }
}
