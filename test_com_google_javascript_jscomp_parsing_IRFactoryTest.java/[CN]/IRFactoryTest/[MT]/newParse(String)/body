{
  CompilerEnvirons environment=new CompilerEnvirons();
  environment.setRecordingComments(true);
  environment.setRecordingLocalJsDocComments(true);
  Parser p=new Parser(environment);
  AstRoot script=p.parse(string,null,0);
  Config config=new Config(registry,ParserRunner.getAnnotationNames(),true);
  Node root=IRFactory.transformTree(script,string,config,new TestErrorReporter(null,null));
  return root;
}
