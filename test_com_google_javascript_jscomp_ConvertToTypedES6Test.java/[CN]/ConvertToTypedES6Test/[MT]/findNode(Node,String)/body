{
  FindNode visitor=new FindNode(identifier);
  NodeTraversal.traverse(compiler,root,visitor);
  Node foundNode=visitor.foundNode;
  assertNotNull("Did not find node named " + identifier + " in "+ root.toStringTree(),foundNode);
  return foundNode;
}
