{
  TypeDeclarationNode actualType=findNode(root,identifier).getDeclaredTypeExpression();
  assertNotNull(identifier + " missing DECLARED_TYPE_EXPR in " + root.toStringTree(),actualType);
  assertTrue(findNode(root,identifier) + " is of type " + actualType+ " not of type "+ expectedType,expectedType.isEquivalentTo(actualType));
}
