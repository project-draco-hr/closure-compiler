{
  Compiler compiler=new Compiler();
  CompilerOptions options=getCompilerOptions();
  compiler.compile(ImmutableList.<SourceFile>of(),ImmutableList.of(SourceFile.fromCode("test",jsInput)),options);
  return compiler;
}
