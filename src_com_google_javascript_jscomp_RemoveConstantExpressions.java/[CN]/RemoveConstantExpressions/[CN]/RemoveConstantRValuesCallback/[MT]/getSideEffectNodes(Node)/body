{
  List<Node> subexpressions=Lists.newArrayList();
  NodeTraversal.traverse(null,node,new GatherSideEffectSubexpressionsCallback(null,new GatherSideEffectSubexpressionsCallback.CopySideEffectSubexpressions(null,subexpressions)));
  List<Node> replacements=Lists.newArrayList();
  for (  Node subexpression : subexpressions) {
    replacements.add(NodeUtil.newExpr(subexpression));
  }
  return replacements;
}
