{
  if (referenced.contains(var)) {
    return;
  }
  referenced.add(var);
  Node parent=var.getParentNode();
  if (parent.getType() == Token.FUNCTION && var.getInitialValue() != var.scope.getRootNode()) {
    traverseFunction(parent,var.scope);
  }
}
