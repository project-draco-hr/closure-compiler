{
  assertEquals("Unexpected warnings:\n" + Joiner.on("\n").join(result.warnings),w,result.warnings.length);
  assertEquals("Unexpected errors:\n" + Joiner.on("\n").join(result.errors),e,result.errors.length);
  assertEquals(e == 0,result.success);
}
