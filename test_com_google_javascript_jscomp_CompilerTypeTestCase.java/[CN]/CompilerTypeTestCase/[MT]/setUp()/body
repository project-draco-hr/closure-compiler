{
  compiler=new Compiler();
  compiler.initCompilerOptionsIfTesting();
  compiler.options_.setWarningLevel(DiagnosticGroups.MISSING_PROPERTIES,CheckLevel.WARNING);
  registry=compiler.getTypeRegistry();
  initTypes();
}
