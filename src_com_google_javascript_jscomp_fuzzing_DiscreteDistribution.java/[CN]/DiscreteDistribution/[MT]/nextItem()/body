{
  final double randomValue=random.nextDouble();
  double sum=0;
  for (int i=0; i < weights.size(); i++) {
    sum+=weights.get(i);
    if (randomValue < sum) {
      return items.get(i);
    }
  }
  return null;
}
