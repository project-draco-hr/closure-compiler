{
switch (n.getToken()) {
case FUNCTION:
    return true;
case HOOK:
  return isSupportedFunctionDefinition(n.getSecondChild()) && isSupportedFunctionDefinition(n.getLastChild());
default :
return false;
}
}
