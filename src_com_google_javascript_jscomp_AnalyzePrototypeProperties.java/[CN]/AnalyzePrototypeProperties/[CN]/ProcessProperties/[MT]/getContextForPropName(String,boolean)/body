{
  NameContext context;
  if (propName != null) {
    context=new NameContext(symbolStack.peek(),getNameInfoForName(propName,PROPERTY),null,resultUsed);
  }
 else {
    context=scopeStack.peek();
  }
  return context;
}
