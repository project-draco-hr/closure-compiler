{
switch (n.getType()) {
case Token.GETPROP:
    visitGetProp(t,n);
  break;
case Token.OBJECTLIT:
visitObjectLit(t,n);
break;
case Token.NAME:
visitName(t,n);
break;
case Token.CALL:
visitCall(t,n);
break;
}
if (isContextIntroducingNode(n)) {
symbolStack.pop();
}
}
