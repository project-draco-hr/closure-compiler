{
  Scope s=t.getScope();
  if (!(s.isGlobal() || s.getDepth() == 2 && s.getRootNode().getParent() == n)) {
    return false;
  }
  return NodeUtil.isFunctionDeclaration(n) || n.isFunction() && n.getParent().isName();
}
