{
  if (t.inGlobalScope() || t.getScopeDepth() == 2 && t.getScopeRoot().getParent() == n) {
    return NodeUtil.isFunctionDeclaration(n) || n.isFunction() && n.getParent().isName();
  }
  return false;
}
