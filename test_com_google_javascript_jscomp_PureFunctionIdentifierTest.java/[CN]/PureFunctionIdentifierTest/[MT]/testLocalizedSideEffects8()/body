{
  checkMarkedCalls("/** @constructor A */ function A() {};" + "function f() {" + "  var a = new A; a.foo = 1; return a;"+ "}"+ "f()",BROKEN_NEW ? ImmutableList.<String>of("A") : ImmutableList.<String>of("A","f"));
}
