{
  checkMarkedCalls("/** @constructor A */ function A() {this.x = 1};" + "function f() {" + "  var a = new A; a.foo = 1; return a;"+ "}"+ "f()",BROKEN_NEW ? ImmutableList.<String>of("A") : ImmutableList.<String>of("A","f"));
}
