{
  return Joiner.on("\n\n").join(Lists.transform(templates,new Function<RefasterJsTemplate,String>(){
    @Override public String apply(    RefasterJsTemplate template){
      return template.toAstString();
    }
  }
));
}
