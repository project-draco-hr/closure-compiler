{
  if (vars.containsKey(name)) {
    return true;
  }
  Scope parent=getParent();
  if (parent != null && parent.getRootNode().isFunction() && parent.isDeclared(name,false)) {
    return true;
  }
  return false;
}
