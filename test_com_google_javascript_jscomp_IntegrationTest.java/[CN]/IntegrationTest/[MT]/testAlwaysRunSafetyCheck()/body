{
  Multimap<CustomPassExecutionTime,CompilerPass> custom=new ImmutableMultimap.Builder<CustomPassExecutionTime,CompilerPass>().put(CustomPassExecutionTime.BEFORE_OPTIMIZATIONS,new CompilerPass(){
    @Override public void process(    Node externs,    Node root){
      Node var=root.getLastChild().getFirstChild();
      assertEquals(Token.VAR,var.getType());
      var.detachFromParent();
    }
  }
).build();
  CompilerOptions options=createCompilerOptions();
  options.setCheckSymbols(false);
  options.setCustomPasses(custom);
  try {
    test(options,"var x = 3; function f() { return x + z; }","function f() { return x + z; }");
    fail("Expected run-time exception");
  }
 catch (  RuntimeException e) {
    assertThat(e.getMessage()).contains("Unexpected variable x");
  }
}
