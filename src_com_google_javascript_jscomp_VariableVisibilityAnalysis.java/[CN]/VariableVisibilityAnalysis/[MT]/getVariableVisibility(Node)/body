{
  Node parent=declaringNameNode.getParent();
  Preconditions.checkArgument(NodeUtil.isVar(parent) || NodeUtil.isFunction(parent) || parent.getType() == Token.LP);
  return visibilityByDeclaringNameNode.get(declaringNameNode);
}
