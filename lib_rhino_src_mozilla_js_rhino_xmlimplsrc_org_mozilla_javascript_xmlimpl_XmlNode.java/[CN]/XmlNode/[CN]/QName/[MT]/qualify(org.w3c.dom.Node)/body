{
  if (namespace.getPrefix() == null) {
    if (node != null) {
      lookupPrefix(node);
    }
 else {
      if (namespace.getUri().equals("")) {
        namespace.setPrefix("");
      }
 else {
        namespace.setPrefix("");
      }
    }
  }
  return qualify(namespace.getPrefix(),localName);
}
