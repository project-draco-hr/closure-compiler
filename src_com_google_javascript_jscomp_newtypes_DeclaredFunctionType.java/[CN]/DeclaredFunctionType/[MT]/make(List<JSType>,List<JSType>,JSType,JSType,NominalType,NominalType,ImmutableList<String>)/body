{
  if (requiredFormals == null) {
    requiredFormals=Lists.newArrayList();
  }
  if (optionalFormals == null) {
    optionalFormals=Lists.newArrayList();
  }
  return new DeclaredFunctionType(requiredFormals,optionalFormals,restFormals,retType,klass,receiverType,typeParameters);
}
