{
  test("/** @define {number|boolean} */ var DEF=false;DEF=true;DEF=3","var DEF=3;true;3");
  Name def=namespace.getNameIndex().get("DEF");
  assertEquals(0,def.refs.size());
  assertEquals(1,def.globalSets);
  assertNotNull(def.declaration);
}
