{
  String callName=call.getFirstChild().getQualifiedName();
  if (callName == null) {
    return false;
  }
  String shortFunctionName=enclosingFunctionName.substring(enclosingFunctionName.lastIndexOf('.') + 1);
  if (callName.endsWith(DOT_JOINER.join(SUPERCLASS_MARKER,shortFunctionName,"call"))) {
    return true;
  }
  if (!callName.endsWith(DOT_JOINER.join(PROTOTYPE_MARKER,shortFunctionName,"call"))) {
    return false;
  }
  String subclass=enclosingFunctionName.substring(0,enclosingFunctionName.indexOf(PROTOTYPE_MARKER));
  String calledClass=callName.substring(0,callName.indexOf(PROTOTYPE_MARKER));
  JSType subclassType=compiler.getTypeIRegistry().getType(subclass);
  JSType calledClassType=compiler.getTypeIRegistry().getType(calledClass);
  if (subclassType == null || calledClassType == null) {
    return false;
  }
  return subclassType.getLeastSupertype(calledClassType) == calledClassType;
}
