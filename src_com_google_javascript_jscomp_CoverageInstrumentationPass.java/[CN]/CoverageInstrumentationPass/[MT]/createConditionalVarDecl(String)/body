{
  Node var=IR.var(IR.name(name),IR.or(IR.name(name),IR.arraylit()));
  JSDocInfoBuilder builder=new JSDocInfoBuilder(false);
  builder.recordSuppressions(ImmutableSet.of("duplicate"));
  var.setJSDocInfo(builder.build());
  return var;
}
