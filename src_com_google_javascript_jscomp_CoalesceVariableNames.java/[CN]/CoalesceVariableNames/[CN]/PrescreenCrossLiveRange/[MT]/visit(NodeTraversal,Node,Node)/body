{
  if (!NodeUtil.isName(n) || !var.name.equals(n.getString())) {
    return;
  }
  count++;
  if (((NodeUtil.isAssign(parent) && parent.getFirstChild() == n) || (NodeUtil.isVar(parent) && n.hasChildren())) && !lattice.isLive(var)) {
    hasDeadAssignment=true;
  }
}
