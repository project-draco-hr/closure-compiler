{
  if (object instanceof NodeMismatch) {
    NodeMismatch that=(NodeMismatch)object;
    return that.nodeActual.equals(this.nodeActual) && that.nodeExpected.equals(this.nodeExpected);
  }
  return false;
}
