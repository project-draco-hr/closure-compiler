{
  Preconditions.checkState(currentLoopContext.get(0).continueCase != -1);
  enclosingBlock.addChildToBack(createStateUpdate(currentLoopContext.get(0).continueCase));
  enclosingBlock.addChildToBack(createSafeBreak());
}
