{
  if (!currentLoopEndCase.isEmpty() && currentLoopEndCase.get(0) == currentStatement.getDouble()) {
    currentLoopEndCase.remove(0);
    currentLoopContinueStatement.remove(0);
    currentBreakCaptureIsSwitch.remove(0);
  }
}
