{
  while (expr != null) {
    if (!expr.isExprResult() || !expr.getFirstChild().isCall() || !isCallToNode(expr.getFirstChild(),targetName)) {
      return false;
    }
    expr=expr.getNext();
  }
  return true;
}
