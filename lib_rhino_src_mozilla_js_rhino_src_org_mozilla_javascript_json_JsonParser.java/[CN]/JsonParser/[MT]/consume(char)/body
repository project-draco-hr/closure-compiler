{
  while (pos < length) {
    char c=src.charAt(pos++);
switch (c) {
case ' ':
case '\t':
case '\r':
case '\n':
      break;
default :
    if (c == token) {
      return;
    }
 else {
      throw new ParseException("Expected " + token + " found "+ c);
    }
}
}
}
