{
switch (val.getType()) {
case Token.STRING:
case Token.NUMBER:
case Token.TRUE:
case Token.FALSE:
    return true;
case Token.NEG:
  return val.getFirstChild().isNumber();
default :
return false;
}
}
