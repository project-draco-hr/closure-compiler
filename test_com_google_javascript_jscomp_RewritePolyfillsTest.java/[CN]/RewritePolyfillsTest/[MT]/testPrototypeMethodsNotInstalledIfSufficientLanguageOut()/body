{
  setLanguage(ES6,ES6);
  testSame("x.normalize();");
  assertThat(getInjectedLibraries()).isEmpty();
  testSame("x.endsWith();");
  assertThat(getInjectedLibraries()).isEmpty();
  testSame("x.fill(y);");
  assertThat(getInjectedLibraries()).isEmpty();
  setLanguage(ES5,ES5);
  testSame("x.forEach();");
  assertThat(getInjectedLibraries()).isEmpty();
}
