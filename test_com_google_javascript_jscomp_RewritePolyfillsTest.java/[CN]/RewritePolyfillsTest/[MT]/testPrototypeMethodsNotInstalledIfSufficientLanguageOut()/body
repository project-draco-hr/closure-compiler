{
  setLanguage(ES6,ES6);
  testSame("x.normalize();");
  assertFalse(getLastCompiler().needsEs6Runtime);
  testSame("x.endsWith();");
  assertFalse(getLastCompiler().needsEs6Runtime);
  testSame("x.fill(y);");
  assertFalse(getLastCompiler().needsEs6Runtime);
  setLanguage(ES5,ES5);
  testSame("x.forEach();");
  assertFalse(getLastCompiler().needsEs6Runtime);
}
