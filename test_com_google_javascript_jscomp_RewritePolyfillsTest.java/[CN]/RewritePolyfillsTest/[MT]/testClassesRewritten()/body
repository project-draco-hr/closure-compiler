{
  setLanguage(ES6,ES5);
  test("var m = new Map();","$jscomp.Map$install(); var m = new $jscomp.Map();");
  assertTrue(getLastCompiler().needsEs6Runtime);
  setLanguage(ES6,ES3);
  test("var s = new Set();","$jscomp.Set$install(); var s = new $jscomp.Set();");
  assertTrue(getLastCompiler().needsEs6Runtime);
}
