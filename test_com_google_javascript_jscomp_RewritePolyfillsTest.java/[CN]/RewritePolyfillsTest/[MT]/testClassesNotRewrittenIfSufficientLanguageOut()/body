{
  setLanguage(ES6,ES6);
  testSame("new Proxy();");
  assertThat(getInjectedLibraries()).isEmpty();
  testSame("var m = new Map();");
  assertThat(getInjectedLibraries()).isEmpty();
  testSame("new Set();");
  assertThat(getInjectedLibraries()).isEmpty();
}
