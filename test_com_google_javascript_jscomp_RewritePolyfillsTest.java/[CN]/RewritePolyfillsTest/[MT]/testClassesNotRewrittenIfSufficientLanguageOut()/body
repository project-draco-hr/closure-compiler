{
  setLanguage(ES6,ES6);
  testSame("new Proxy();");
  assertFalse(getLastCompiler().needsEs6Runtime);
  testSame("var m = new Map();");
  assertFalse(getLastCompiler().needsEs6Runtime);
  testSame("new Set();");
  assertFalse(getLastCompiler().needsEs6Runtime);
}
