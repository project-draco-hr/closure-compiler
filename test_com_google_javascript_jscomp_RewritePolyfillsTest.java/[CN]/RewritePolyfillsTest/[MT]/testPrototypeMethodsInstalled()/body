{
  setLanguage(ES6,ES5);
  test("x.endsWith(y);","$jscomp.string.endsWith$install(); x.endsWith(y);");
  assertTrue(getLastCompiler().needsEs6Runtime);
  test("x.fill();","$jscomp.array.fill$install(); x.fill();");
  assertTrue(getLastCompiler().needsEs6Runtime);
  setLanguage(ES5,ES3);
  test("x.forEach(y);","$jscomp.array.forEach$install(); x.forEach(y);");
  assertTrue(getLastCompiler().needsEs6Runtime);
}
