{
  if (left == null) {
    left=operator.hasSideEffect() ? new AssignableExprFuzzer(random,scopeManager,config,snGenerator) : new ExpressionFuzzer(random,scopeManager,config,snGenerator);
  }
  return left;
}
