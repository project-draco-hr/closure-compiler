{
  typeCheck("throw 123;");
  typeCheck("var msg = 'hello'; throw msg;");
  typeCheck(Joiner.on('\n').join("function f(cond, x, y) {","  if (cond) {","    x < y;","    throw 123;","  } else {","    x < 2;","  }","}"));
  typeCheck("throw (1 - 'asdf');",NewTypeInference.INVALID_OPERAND_TYPE);
}
