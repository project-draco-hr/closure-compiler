{
  boolean isAssignTarget=(parent.getType() == Token.ASSIGN) && (parent.getFirstChild() == getPropNode);
  return (propNameNode.getType() == Token.STRING) && isAssignTarget && props.containsKey(propNameNode.getString());
}
