{
  StringBuilder sb=new StringBuilder();
  int ch=-1;
  while ((ch=consumeCharacter()) != '\n') {
    if (ch == -1) {
      return sb.toString();
    }
    sb.append((char)ch);
  }
  return sb.toString();
}
