{
  testTypes("/**\n" + " * @constructor\n" + " * @struct\n"+ " */\n"+ "function Foo() {}\n"+ "(new Foo()).x += 123;",new String[]{"Cannot add a property to a struct instance " + "after it is constructed.","Property x never defined on Foo"});
}
