{
  testTypes(LINE_JOINER.join("/** @constructor */ var C = function() {};","/** @abstract */ C.prototype.foo = function() {};"),"Abstract methods can only appear in abstract classes. Please declare the class as " + "@abstract");
}
