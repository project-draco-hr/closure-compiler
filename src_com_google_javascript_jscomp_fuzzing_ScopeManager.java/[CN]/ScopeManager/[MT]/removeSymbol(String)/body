{
  Symbol symbol=null;
  for (  Symbol s : localSymbols()) {
    if (s.name.equals(symbolName)) {
      symbol=s;
      break;
    }
  }
  if (symbol != null && localSymbols().remove(symbol)) {
    numSym--;
  }
}
