{
  testSame("/** @constructor */" + "function F(a) {" + "  if (a < 10) {"+ "    this.a = a;"+ "  } else {"+ "    this.a = 10;"+ "  }"+ "};");
  testSame("/** @constructor */" + "function F(a) {" + "  if (a < 10) {"+ "    this.a = a;"+ "  }"+ "};",CheckPropertyOrder.UNASSIGNED_PROPERTY);
}
