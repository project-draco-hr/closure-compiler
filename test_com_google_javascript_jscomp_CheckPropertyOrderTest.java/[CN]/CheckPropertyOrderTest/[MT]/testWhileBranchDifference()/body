{
  testSame("var a = {};" + "/** @constructor */" + "a.F = function() {"+ "  this.a = 0;"+ "  while (this.a < 10) {"+ "    this.b = 3;"+ "    ++this.a;"+ "  }"+ "};",CheckPropertyOrder.UNASSIGNED_PROPERTY);
}
