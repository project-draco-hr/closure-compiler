{
  if (isForInOverArray(n)) {
    compiler.report(t.makeError(n,FOR_IN_OVER_ARRAY));
  }
 else   if (isGoogObjectIterationOverArray(n)) {
    compiler.report(t.makeError(n,ARRAY_PASSED_TO_GOOG_OBJECT,n.getFirstChild().getQualifiedName()));
  }
}
