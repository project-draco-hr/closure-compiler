{
  super.enableTypeCheck(CheckLevel.OFF);
  String boilerplate=PROMISE_BOILERPLATE + "/** @type {goog.Promise} */ var y = f();";
  test(boilerplate + "y.then();",boilerplate + "y.thenVoid();");
  test(boilerplate + "y.then(function f(){});",boilerplate + "y.thenVoid(function f(){});");
  test(boilerplate + "y.then(function f(){}, function g() {});",boilerplate + "y.thenVoid(function f(){}, function g() {});");
  test(boilerplate + "y.then().then();",boilerplate + "y.then().thenVoid();");
}
