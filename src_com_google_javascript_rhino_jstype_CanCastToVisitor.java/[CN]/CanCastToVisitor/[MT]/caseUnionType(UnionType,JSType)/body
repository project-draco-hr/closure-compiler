{
  for (  JSType type : thisType.getAlternates()) {
    if (type.isVoidType() || type.isNullType()) {
    }
 else {
      if (type.visit(this,thatType)) {
        return true;
      }
    }
  }
  return false;
}
