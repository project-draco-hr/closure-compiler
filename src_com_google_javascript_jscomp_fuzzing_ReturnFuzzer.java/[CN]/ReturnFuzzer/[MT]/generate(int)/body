{
  Node node=new Node(Token.RETURN);
  if (budget > 1 && context.random.nextDouble() < getOwnConfig().optDouble("hasValue")) {
    node.addChildToBack(new ExpressionFuzzer(context).generate(budget - 1));
  }
  return node;
}
