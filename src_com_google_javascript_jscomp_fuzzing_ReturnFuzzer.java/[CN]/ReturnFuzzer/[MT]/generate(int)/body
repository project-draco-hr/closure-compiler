{
  Node node=new Node(Token.RETURN);
  if (budget > 1 && random.nextDouble() < getOwnConfig().optDouble("hasValue")) {
    node.addChildToBack(new ExpressionFuzzer(random,scopeManager,config,snGenerator).generate(budget - 1));
  }
  return node;
}
