{
switch (n.getType()) {
case Token.SCRIPT:
case Token.BLOCK:
case Token.FUNCTION:
    JSDocInfo info=n.getJSDocInfo();
  if (info != null && info.getSuppressions().contains("unreachable")) {
    return false;
  }
}
return true;
}
