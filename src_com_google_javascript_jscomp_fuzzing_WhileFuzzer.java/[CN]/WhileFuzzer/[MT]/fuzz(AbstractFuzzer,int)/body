{
  if (fuzzer instanceof ExpressionFuzzer) {
    Set<Type> types=Sets.immutableEnumSet(Sets.difference(fuzzer.supportedTypes(),EnumSet.of(Type.UNDEFINED)));
    return fuzzer.generate(budget,types);
  }
 else {
    return fuzzer.generate(budget);
  }
}
