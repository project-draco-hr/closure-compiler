{
  if (n.getType() == Token.CALL) {
    Node first=n.getFirstChild();
    if (first.getType() == Token.NAME && first.getString().equals(callname)) {
      complete=method.call(t,n,parent);
    }
  }
  if (parent == null) {
    assertTrue(complete);
  }
}
