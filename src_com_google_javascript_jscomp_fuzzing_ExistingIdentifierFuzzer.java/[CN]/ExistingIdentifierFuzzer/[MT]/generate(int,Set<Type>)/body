{
  Preconditions.checkState(isEnough(1),"No symbol defined.");
  int flags=0;
  if (excludeExterns) {
    flags=ScopeManager.EXCLUDE_EXTERNS;
  }
  return Node.newString(Token.NAME,context.scopeManager.getRandomSymbol(type,flags).name);
}
