{
  mode=LanguageMode.ECMASCRIPT6;
  expectedFeatures=FeatureSet.ES6;
  parse("({x: y} = z);");
  parse("({x: (y)} = z);");
  parse("({x: ((y))} = z);");
  parse("([x] = y);");
  parse("[(x), y] = z;");
  parse("[x, (y)] = z;");
  parse("[x, ([y])] = z;");
  parse("[x, (([y]))] = z;");
}
