{
  if (NodeUtil.isClassDeclaration(classNode) || parent.isName() || (parent.isAssign() && parent.getParent().isExprResult())) {
    return false;
  }
  return expressionDecomposer.canExposeExpression(classNode) == DecompositionType.MOVABLE;
}
