{
  testIR(IR.paramList(),"PARAM_LIST\n");
  testIR(IR.paramList(IR.name("a"),IR.name("b")),"PARAM_LIST\n" + "    NAME a\n" + "    NAME b\n");
  testIR(IR.paramList(ImmutableList.of(IR.name("a"),IR.name("b"))),"PARAM_LIST\n" + "    NAME a\n" + "    NAME b\n");
}
