{
  if (messageNames.containsKey(msgName)) {
    MessageLocation location=messageNames.get(msgName);
    compiler.report(JSError.make(sourceName,msgNode,MESSAGE_DUPLICATE_KEY,msgName,location.sourceName,Integer.toString(location.lineNo)));
  }
 else {
    MessageLocation location=new MessageLocation(sourceName,msgNode.getLineno());
    messageNames.put(msgName,location);
  }
}
