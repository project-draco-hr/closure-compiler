{
  ComposeWarningsGuard guard=new ComposeWarningsGuard(accessControlsOff,accessControlsWarning);
  guard.addGuard(guard);
  assertEquals("DiagnosticGroup<accessControls>(WARNING), " + "DiagnosticGroup<accessControls>(OFF)",guard.toString());
}
