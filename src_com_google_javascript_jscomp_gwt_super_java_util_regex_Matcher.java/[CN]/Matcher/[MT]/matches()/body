{
  result=regExp.exec(input);
  hasExecuted=true;
  findFromIndex=0;
  if (result != null) {
    String match=result.getGroup(0);
    if (match.equals(input)) {
      return true;
    }
    result=null;
  }
  return false;
}
