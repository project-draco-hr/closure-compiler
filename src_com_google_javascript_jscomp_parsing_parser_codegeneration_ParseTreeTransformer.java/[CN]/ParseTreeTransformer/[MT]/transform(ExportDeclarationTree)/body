{
  ParseTree declaration=transformAny(tree.declaration);
  if (tree.declaration == declaration) {
    return tree;
  }
  return new ExportDeclarationTree(null,tree.isDefault,tree.isExportAll,declaration,tree.exportSpecifierList,tree.from);
}
