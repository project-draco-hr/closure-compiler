{
  testSame("try { } finally {  }");
  testSame("try { foo(); } finally { bar() } ");
  testSame("try { foo() } finally { bar() }");
  testSame("try { foo(); } catch (e) {e()} finally { bar() }");
  testSame("try { foo() } catch (e) {e()} finally { bar() }");
}
