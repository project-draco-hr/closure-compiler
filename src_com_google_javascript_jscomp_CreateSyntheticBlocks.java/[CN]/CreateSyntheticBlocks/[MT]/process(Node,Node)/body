{
  NodeTraversal.traverse(compiler,root,new Callback());
  for (  StartMarker startMarker : startMarkerStack) {
    compiler.report(JSError.make(startMarker.sourceName,startMarker.node,UNMATCHED_START_MARKER,startMarkerName));
  }
}
