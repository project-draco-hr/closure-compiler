{
  testWarning(LINE_JOINER.join("/**"," * @return {void}"," */","function f(namedParam, {destructuring:pattern} = defaultValue) {","}"),MISSING_PARAM_JSDOC);
  testWarning(LINE_JOINER.join("/**"," * @return {void}"," */","function f(namedParam, [pattern] = defaultValue) {","}"),MISSING_PARAM_JSDOC);
}
