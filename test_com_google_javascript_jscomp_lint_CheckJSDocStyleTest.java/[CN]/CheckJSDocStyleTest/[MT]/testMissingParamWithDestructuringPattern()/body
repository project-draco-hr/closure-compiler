{
  testWarning(LINE_JOINER.join("/**"," * @return {void}"," */","function f(namedParam, {destructuring:pattern}) {","}"),MISSING_PARAM_JSDOC);
  testWarning(LINE_JOINER.join("/**"," * @return {void}"," */","function f({destructuring:pattern}, namedParam) {","}"),MISSING_PARAM_JSDOC);
  testWarning(LINE_JOINER.join("/**"," * @return {void}"," */","function f(namedParam, [pattern]) {","}"),MISSING_PARAM_JSDOC);
  testWarning(LINE_JOINER.join("/**"," * @return {void}"," */","function f([pattern], namedParam) {","}"),MISSING_PARAM_JSDOC);
}
