{
  Node key=n.getFirstChild();
  while (key != null) {
    if (key.getType() != Token.NUMBER && isStripName(key.getString())) {
      Node value=key.getFirstChild();
      Node next=key.getNext();
      n.removeChild(key);
      key=next;
      compiler.reportCodeChange();
    }
 else {
      key=key.getNext();
    }
  }
}
