{
  if (n.getParent().getLastChild() != n) {
    reportCodeChange();
    while (n.getNext() != null) {
      Node dead=n.getNext();
      NodeUtil.redeclareVarsInsideBranch(dead);
      dead.detach();
    }
  }
}
