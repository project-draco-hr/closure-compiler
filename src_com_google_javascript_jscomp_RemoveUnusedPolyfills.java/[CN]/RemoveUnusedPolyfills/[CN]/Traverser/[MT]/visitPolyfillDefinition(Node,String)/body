{
  PrototypeMethod method=PrototypeMethod.split(polyfill);
  if (method != null) {
    methodPolyfills.put(method,node);
    methodsByName.put(method.method,method);
  }
 else {
    staticPolyfills.put(polyfill,node);
  }
}
