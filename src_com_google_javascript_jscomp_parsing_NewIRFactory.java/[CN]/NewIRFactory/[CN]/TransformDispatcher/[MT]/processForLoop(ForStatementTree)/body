{
  Node node=newNode(Token.FOR,tranformOrEmpty(loopNode.initializer),tranformOrEmpty(loopNode.condition),tranformOrEmpty(loopNode.increment));
  node.addChildToBack(transformBlock(loopNode.body));
  return node;
}
