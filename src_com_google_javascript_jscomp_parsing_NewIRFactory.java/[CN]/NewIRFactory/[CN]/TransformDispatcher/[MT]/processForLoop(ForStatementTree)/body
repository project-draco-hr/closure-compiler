{
  Node node=newNode(Token.FOR,transformOrEmpty(loopNode.initializer),transformOrEmpty(loopNode.condition),transformOrEmpty(loopNode.increment));
  node.addChildToBack(transformBlock(loopNode.body));
  return node;
}
