{
  Node node;
  if (asString) {
    node=newStringNode(Token.STRING,identifierToken.value);
  }
 else {
    JSDocInfo info=handleJsDoc(identifierToken);
    if (identifierToken == null || isReservedKeyword(identifierToken.toString())) {
      int linenum=identifierToken != null ? identifierToken.location.start.line : 0;
      errorReporter.error("identifier is a reserved word",sourceName,linenum,"",0);
    }
    node=newStringNode(Token.NAME,identifierToken.value);
    if (info != null) {
      attachJSDoc(info,node);
    }
  }
  setSourceInfo(node,identifierToken);
  return node;
}
