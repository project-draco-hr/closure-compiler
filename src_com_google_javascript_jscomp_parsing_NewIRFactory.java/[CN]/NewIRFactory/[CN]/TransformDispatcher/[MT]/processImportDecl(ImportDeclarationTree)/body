{
  maybeWarnEs6Feature(tree,"modules");
  Node export=newNode(Token.IMPORT,transformOrEmpty(tree.defaultBindingIndentifier),transformListOrEmpty(Token.IMPORT_SPECS,tree.importSpecifierList),processString(tree.moduleSpecifier));
  return export;
}
