{
  maybeWarnEs6Feature(tree,"modules");
  return newNode(Token.IMPORT,transformOrEmpty(tree.defaultBindingIndentifier,tree),transformListOrEmpty(Token.IMPORT_SPECS,tree.importSpecifierList),processString(tree.moduleSpecifier));
}
