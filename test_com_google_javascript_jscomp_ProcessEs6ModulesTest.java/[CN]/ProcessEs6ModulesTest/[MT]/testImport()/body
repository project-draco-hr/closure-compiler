{
  test("import name from 'test'; use(name);",Joiner.on('\n').join(FILEOVERVIEW,"goog.require('module$test');","use(module$test.default);"));
  test("import {n as name} from 'test';",FILEOVERVIEW + "goog.require('module$test');");
  test("import x, {f as foo, b as bar} from 'test'; use(x);",Joiner.on('\n').join(FILEOVERVIEW,"goog.require('module$test');","use(module$test.default);"));
}
