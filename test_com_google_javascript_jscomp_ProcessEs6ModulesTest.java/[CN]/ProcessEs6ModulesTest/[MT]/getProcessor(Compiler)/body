{
  return new CompilerPass(){
    @Override public void process(    Node externs,    Node root){
      NodeTraversal.traverse(compiler,root,new ProcessEs6Modules(compiler,ES6ModuleLoader.createNaiveLoader(compiler,"foo/bar/"),false));
    }
  }
;
}
