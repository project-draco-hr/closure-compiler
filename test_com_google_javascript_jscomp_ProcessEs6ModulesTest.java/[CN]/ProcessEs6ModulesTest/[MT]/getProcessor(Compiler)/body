{
  return new CompilerPass(){
    @Override public void process(    Node externs,    Node root){
      NodeTraversal.traverse(compiler,root,new ProcessEs6Modules(compiler,new ES6ModuleLoader(compiler,"foo/bar/"),false));
    }
  }
;
}
