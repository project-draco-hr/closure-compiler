{
switch (n.getType()) {
case Token.CALL:
case Token.NEW:
    return true;
case Token.NAME:
  return !NodeUtil.isConstantName(n) && !additionalConsts.contains(n.getString());
default :
return false;
}
}
