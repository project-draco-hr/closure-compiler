{
  if (this == that) {
    return true;
  }
 else   if (this.isNominalType() && that instanceof JSType && ((JSType)that).isNominalType()) {
    return getReferenceName().equals(((ObjectType)that).getReferenceName());
  }
  return false;
}
