{
  if (budget < 4) {
    return generateStatement(budget);
  }
  if (random.nextInt(2) == 0) {
    return generateStatement(budget);
  }
 else {
    return generateFunctionDeclaration(budget);
  }
}
