{
  int[] subBudgets=new int[n];
  for (int i=0; i < n; i++) {
    subBudgets[i]=1;
  }
  budget-=n;
  while (budget > 0) {
    subBudgets[random.nextInt(n)]++;
    budget--;
  }
  return subBudgets;
}
