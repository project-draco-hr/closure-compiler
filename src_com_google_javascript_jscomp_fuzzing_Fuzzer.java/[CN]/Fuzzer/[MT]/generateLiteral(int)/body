{
  int rand;
  if (budget > 1) {
    rand=random.nextInt(7);
  }
 else   if (budget == 1) {
    rand=random.nextInt(6);
  }
 else {
    rand=-1;
  }
switch (rand) {
case 0:
    return generateNullLiteral(budget);
case 1:
  return generateBooleanLiteral(budget);
case 2:
return generateNumericLiteral(budget);
case 3:
return generateStringLiteral(budget);
case 4:
return generateArrayLiteral(budget);
case 5:
return generateObjectLiteral(budget);
case 6:
return generateRegularExpressionLiteral(budget);
default :
return null;
}
}
