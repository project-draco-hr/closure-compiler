{
  List<Symbol> result=Lists.newArrayList();
  for (  Symbol symbol : table.getAllSymbols()) {
    if (symbol.getDeclaration() != null && !symbol.getDeclaration().getSourceFile().isExtern()) {
      result.add(symbol);
    }
  }
  return result;
}
