{
  NodeTraversal.traverseRoots(compiler,Lists.newArrayList(externs,root),new DetectLoopClosures());
  NodeTraversal.traverseRoots(compiler,Lists.newArrayList(externs,root),this);
  NodeTraversal.traverseRoots(compiler,Lists.newArrayList(externs,root),this.new RenameReferences());
}
