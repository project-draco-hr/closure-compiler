{
  ProcessClosurePrimitives closurePass=new ProcessClosurePrimitives(compiler,true,true);
  closurePass.process(externs,root);
  renameVars=new RenameVars(compiler,prefix,false,previouslyUsedMap,null,closurePass.getExportedVariableNames());
  renameVars.process(externs,root);
}
