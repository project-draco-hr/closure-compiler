{
  if (withClosurePass) {
    return new ClosurePassAndRenameVars(compiler);
  }
 else {
    return renameVars=new RenameVars(compiler,prefix,localRenamingOnly,preserveAnonymousFunctionNames,generatePseudoNames,previouslyUsedMap,null,null);
  }
}
