{
  test("var walk = function walk(node, aFunction) {" + "  walk(node, aFunction);" + "};","var d = function a(b, c) {" + "  a(b, c);" + "};");
  localRenamingOnly=true;
  test("var walk = function walk(node, aFunction) {" + "  walk(node, aFunction);" + "};","var walk = function a(b, c) {" + "  a(b, c);" + "};");
}
