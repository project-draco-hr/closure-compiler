{
  PolymerClassDefinition def=new PolymerClassDefinition(null,IR.objectlit(),null,null,"input",null,null);
  assertThat(PolymerPassStaticUtils.getPolymerElementType(def)).isEqualTo("PolymerInputElement");
}
