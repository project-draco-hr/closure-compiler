{
  super.setUp();
  compiler.getOptions().setLanguageIn(LanguageMode.ECMASCRIPT6_TYPED);
  passes.add(makePassFactory("convertDeclaredTypesToJSDoc",new ConvertDeclaredTypesToJSDoc(compiler)));
  addES6TranspilationPasses();
}
