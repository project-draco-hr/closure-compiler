{
switch (attribute) {
case CONSTANT:
    return inferredType.toString() + '^';
case REQUIRED:
  return inferredType.toString();
case OPTIONAL:
return inferredType.toString() + '=';
default :
throw new RuntimeException("Unknown Attribute value " + attribute);
}
}
