{
  if (!inferredType.unifyWithSubtype(other.inferredType,typeParameters,typeMultimap)) {
    return false;
  }
 else   if (declaredType != null && other.declaredType != null && !declaredType.unifyWithSubtype(other.declaredType,typeParameters,typeMultimap)) {
    return false;
  }
  return true;
}
