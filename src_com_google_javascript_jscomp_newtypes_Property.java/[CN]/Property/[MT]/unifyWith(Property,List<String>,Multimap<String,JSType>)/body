{
  if (!inferredType.unifyWith(other.inferredType,typeParameters,typeMultimap)) {
    return false;
  }
 else   if (declaredType != null && other.declaredType != null && !declaredType.unifyWith(other.declaredType,typeParameters,typeMultimap)) {
    return false;
  }
  return true;
}
