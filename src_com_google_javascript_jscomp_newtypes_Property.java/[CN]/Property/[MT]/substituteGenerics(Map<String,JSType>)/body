{
  if (concreteTypes.isEmpty()) {
    return this;
  }
  return new Property(inferredType.substituteGenerics(concreteTypes),declaredType == null ? null : declaredType.substituteGenerics(concreteTypes),attribute);
}
