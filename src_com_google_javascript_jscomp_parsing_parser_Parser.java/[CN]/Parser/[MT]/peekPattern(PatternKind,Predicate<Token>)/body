{
  if (!peekPatternStart()) {
    return false;
  }
  Parser p=createLookaheadParser();
  try {
    p.parsePattern(kind);
    return follow.apply(p.peekToken());
  }
 catch (  ParseException e) {
    return false;
  }
}
