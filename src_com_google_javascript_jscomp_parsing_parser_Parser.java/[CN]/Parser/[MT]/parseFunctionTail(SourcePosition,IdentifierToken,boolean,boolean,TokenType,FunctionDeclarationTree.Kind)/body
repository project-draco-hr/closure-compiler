{
  inGeneratorContext.addLast(isGenerator);
  FunctionDeclarationTree.Builder builder=FunctionDeclarationTree.builder(kind).setName(name).setStatic(isStatic).setGenerator(isGenerator).setAccess(access).setGenerics(maybeParseGenericTypes()).setFormalParameterList(parseFormalParameterList(ParamContext.IMPLEMENTATION)).setReturnType(maybeParseColonType()).setFunctionBody(parseFunctionBody());
  inGeneratorContext.removeLast();
  return builder.build(getTreeLocation(start));
}
