{
  int last=(lookahead == 0) ? getLastLine() : peekToken(lookahead - 1).location.end.line;
  int next=peekToken(lookahead).location.start.line;
  return next > last || peek(lookahead,TokenType.SEMI_COLON) || peek(lookahead,TokenType.CLOSE_CURLY) || peek(lookahead,TokenType.END_OF_FILE);
}
