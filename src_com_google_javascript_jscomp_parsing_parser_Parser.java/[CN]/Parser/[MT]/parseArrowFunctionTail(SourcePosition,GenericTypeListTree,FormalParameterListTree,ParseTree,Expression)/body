{
  inGeneratorContext.addLast(false);
  if (peekImplicitSemiColon()) {
    reportError("No newline allowed before '=>'");
  }
  eat(TokenType.ARROW);
  ParseTree functionBody;
  if (peek(TokenType.OPEN_CURLY)) {
    functionBody=parseFunctionBody();
  }
 else {
    functionBody=parseAssignment(expressionIn,false);
  }
  FunctionDeclarationTree declaration=new FunctionDeclarationTree(getTreeLocation(start),null,generics,false,false,false,null,FunctionDeclarationTree.Kind.ARROW,formalParameterList,returnType,functionBody);
  inGeneratorContext.removeLast();
  return declaration;
}
