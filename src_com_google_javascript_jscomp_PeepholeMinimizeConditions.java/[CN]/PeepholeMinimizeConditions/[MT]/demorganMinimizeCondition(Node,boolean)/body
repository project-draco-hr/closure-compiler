{
  MinimizedCondition minCond=MinimizedCondition.fromConditionNode(n);
  if (countLeadingNot || minCond.getLength() <= minCond.getNegativeLength()) {
    return maybeReplaceNode(n,minCond.getNode());
  }
 else {
    return maybeReplaceNode(n,new Node(Token.NOT,minCond.getNegatedNode()));
  }
}
