{
switch (name.getType()) {
case Token.SET:
case Token.GET:
case Token.STRING:
    return new Node(Token.GETPROP,new Node(Token.OBJECTLIT),name.cloneNode());
default :
  throw new IllegalStateException("unexpected");
}
}
