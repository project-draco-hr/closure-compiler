{
  super(inExterns);
  Preconditions.checkNotNull(lValue);
  Preconditions.checkArgument(ALLOWED_TYPES.contains(lValue.getType()),"Unexpected lValue type %s",lValue.getType());
  this.lValue=lValue;
}
