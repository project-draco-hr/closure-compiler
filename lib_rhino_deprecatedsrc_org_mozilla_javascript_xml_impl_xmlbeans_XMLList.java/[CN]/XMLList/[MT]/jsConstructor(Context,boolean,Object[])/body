{
  if (args.length == 0) {
    return new XMLList(lib);
  }
 else {
    Object arg0=args[0];
    if (!inNewExpr && arg0 instanceof XMLList) {
      return arg0;
    }
    return new XMLList(lib,arg0);
  }
}
