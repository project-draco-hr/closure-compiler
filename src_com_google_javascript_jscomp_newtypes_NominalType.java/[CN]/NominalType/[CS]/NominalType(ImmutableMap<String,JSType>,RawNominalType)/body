{
  Preconditions.checkState(typeMap.isEmpty() || typeMap.keySet().containsAll(rawType.templateVars) && rawType.templateVars.containsAll(typeMap.keySet()));
  this.typeMap=typeMap;
  this.rawType=rawType;
}
