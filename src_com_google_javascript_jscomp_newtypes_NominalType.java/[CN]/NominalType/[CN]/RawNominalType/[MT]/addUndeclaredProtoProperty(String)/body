{
  Preconditions.checkState(!isFinalized);
  if (!protoProps.containsKey(pname) || protoProps.get(pname).getDeclaredType() == null) {
    protoProps=protoProps.with(pname,Property.make(JSType.UNKNOWN,null));
  }
}
