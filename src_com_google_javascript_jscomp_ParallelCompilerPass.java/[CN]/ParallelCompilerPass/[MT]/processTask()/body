{
  Node subtree=getTask();
  try {
    if (subtree == null) {
      return null;
    }
 else {
      return taskSupply.get().processSubtree(subtree);
    }
  }
 catch (  Exception e) {
    Result r=new Result(true);
    r.exceptions.add(e);
    return r;
  }
}
