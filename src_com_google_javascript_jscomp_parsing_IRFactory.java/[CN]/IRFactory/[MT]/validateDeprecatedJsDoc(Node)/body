{
  JSDocInfo info=n.getJSDocInfo();
  if (info == null) {
    return;
  }
  if (info.isExpose()) {
    errorReporter.warning(String.format(ANNOTATION_DEPRECATED,"@expose"," Use @nocollapse or @export instead."),sourceName,n.getLineno(),n.getCharno());
  }
}
