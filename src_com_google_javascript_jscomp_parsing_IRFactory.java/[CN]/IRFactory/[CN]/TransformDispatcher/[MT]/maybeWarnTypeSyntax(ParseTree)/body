{
  if (config.languageMode != LanguageMode.ECMASCRIPT6_TYPED) {
    errorReporter.warning("type syntax is only supported in ES6 typed mode",sourceName,lineno(node),charno(node));
  }
  recordTypeSyntax(node.location);
}
