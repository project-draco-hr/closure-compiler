{
  assertNotNull(left);
  assertNotNull(right);
  int beg=left.getPosition();
  int end=right.getPosition() + right.getLength();
  setBounds(beg,end);
  setLineno(left.getLineno());
  setLeft(left);
  setRight(right);
}
