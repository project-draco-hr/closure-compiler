{
  if (moduleType != type && moduleType != ModuleType.NON_MODULE) {
    errorManager.report(CheckLevel.WARNING,JSError.make(ModuleLoader.MODULE_CONFLICT,fileUri.toString()));
  }
  moduleType=type;
}
