{
  strings=ImmutableSet.of("",",","overimaginative");
  testSame("alert('')");
  testSame("var a=[1,2,3];a.join(',')");
  test("f('overimaginative', 'overimaginative')","var $$S_overimaginative='overimaginative';" + "f($$S_overimaginative,$$S_overimaginative)");
}
