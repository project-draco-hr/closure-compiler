{
  if (!type.isEmptyType() && !castType.isEmptyType() && type.getGreatestSubtype(castType).isEmptyType()) {
    registerMismatch(type,castType,report(t.makeError(n,INVALID_CAST,castType.toString(),type.toString())));
  }
}
