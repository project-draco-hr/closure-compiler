{
  Compiler compiler=new Compiler();
  compiler.init(new ArrayList<SourceFile>(),new ArrayList<SourceFile>(),new CompilerOptions());
  Node base=compiler.loadLibraryCode("base",true);
  Node typeCheck=compiler.loadLibraryCode("runtime_type_check",true);
  test(js,compiler.toSource(base) + ";" + compiler.toSource(typeCheck)+ ";"+ expected);
}
