{
  Compiler compiler=new Compiler();
  compiler.init(Lists.<SourceFile>newArrayList(),Lists.<SourceFile>newArrayList(),new CompilerOptions());
  Node boilerplateCode=RuntimeTypeCheck.getBoilerplateCode(compiler,null);
  test(js,compiler.toSource(boilerplateCode) + ";" + expected);
}
