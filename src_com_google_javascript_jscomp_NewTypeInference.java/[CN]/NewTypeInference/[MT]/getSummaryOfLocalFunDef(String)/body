{
  JSType summaryType=summaries.get(currentScope.getScope(name));
  if (summaryType == null) {
    summaryType=currentScope.getDeclaredTypeOf(name);
    Preconditions.checkState(summaryType.getFunType() != null,"Needed function but found %s",summaryType);
  }
  return summaryType;
}
