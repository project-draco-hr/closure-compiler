{
  if (commonTypes.getArrayInstance().isUnknown() || t.isUnknown() || t.isLoose()|| t.isEnumElement() && t.getEnumeratedType().isUnknown()) {
    return false;
  }
  return t.isSubtypeOf(commonTypes.getArrayInstance());
}
