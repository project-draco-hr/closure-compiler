{
  Node n=cfg.getImplicitReturn().getValue();
  if (cfg.getInEdges(n).size() == 0) {
    TypeEnv e=new TypeEnv();
    return envPutType(e,RETVAL_ID,TypeConsts.BOTTOM);
  }
 else {
    return getInEnv(n);
  }
}
