{
  boolean fuzzyDeclaration=declared == null || declared.isUnknown() || (declared.isTop() && !inferred.isTop());
  return fuzzyDeclaration && inferred.isFromUndeclaredFormal() && required.isNonLooseSubtypeOf(inferred);
}
