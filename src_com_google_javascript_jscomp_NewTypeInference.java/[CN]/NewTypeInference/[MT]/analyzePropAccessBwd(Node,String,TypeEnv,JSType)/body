{
  EnvTypePair pair=analyzeExprBwd(receiver,outEnv,JSType.TOP_OBJECT.withProperty(pname,requiredType));
  JSType receiverType=pair.type;
  JSType propAccessType=receiverType.mayHaveProp(pname) ? receiverType.getProp(pname) : requiredType;
  pair.type=propAccessType;
  return pair;
}
