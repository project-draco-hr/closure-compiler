{
  Preconditions.checkArgument(getProp.isGetProp());
  if (recvType.mayBeStruct() && !recvType.isLooseStruct() && !recvType.hasProp(pname)) {
    warnings.add(JSError.make(getProp,ILLEGAL_PROPERTY_CREATION));
    return true;
  }
  return false;
}
