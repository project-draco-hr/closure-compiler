{
  return lhs.isBottom() || rhs.isBottom() || lhs.isUnknown()|| rhs.isUnknown()|| lhs.isBoolean() && rhs.isBoolean() || lhs.isSubtypeOf(JSType.NUMBER) && rhs.isSubtypeOf(JSType.NUMBER) || lhs.isSubtypeOf(JSType.STRING) && rhs.isSubtypeOf(JSType.STRING) || lhs.equals(rhs);
}
