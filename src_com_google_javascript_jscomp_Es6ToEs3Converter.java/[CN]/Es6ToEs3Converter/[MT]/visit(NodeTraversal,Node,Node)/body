{
switch (n.getType()) {
case Token.STRING_KEY:
    visitStringKey(n);
  break;
case Token.CLASS:
visitClass(n);
break;
case Token.SUPER:
case Token.SPREAD:
cannotConvert(n,Token.name(n.getType()));
break;
}
}
