{
switch (n.getType()) {
case Token.STRING_KEY:
    visitStringKey(n);
  break;
case Token.CLASS:
visitClass(n,parent);
break;
case Token.SUPER:
case Token.SPREAD:
cannotConvertYet(n,Token.name(n.getType()));
break;
}
}
