{
  String propertyName=getprop.getLastChild().getString();
  return (isPrivateByConvention(propertyName)) ? Visibility.PRIVATE : Optional.fromNullable(objectType).transform(getVisibilityFromObjectType(propertyName)).or(Optional.of(Visibility.INHERITED)).transform(maybeUpgradeToFileOverviewVisibility).get();
}
