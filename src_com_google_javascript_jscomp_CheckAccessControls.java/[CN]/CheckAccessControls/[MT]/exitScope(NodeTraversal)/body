{
  if (!t.inGlobalScope()) {
    Node n=t.getScopeRoot();
    if (isDeprecatedFunction(n)) {
      deprecatedDepth--;
    }
    methodDepth--;
    if (methodDepth == 0) {
      currentClass=null;
    }
  }
}
