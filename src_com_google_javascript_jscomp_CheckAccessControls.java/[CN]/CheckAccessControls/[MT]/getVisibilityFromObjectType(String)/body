{
  return new Function<ObjectType,Visibility>(){
    @Override public Visibility apply(    ObjectType objectType){
      return objectType.getOwnPropertyJSDocInfo(propertyName).getVisibility();
    }
  }
;
}
