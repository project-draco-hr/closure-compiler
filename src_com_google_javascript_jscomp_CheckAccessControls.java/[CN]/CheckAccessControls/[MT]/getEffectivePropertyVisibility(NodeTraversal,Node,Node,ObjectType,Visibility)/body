{
  boolean isOverride=parent.getJSDocInfo() != null && parent.isAssign() && parent.getFirstChild() == getprop;
  return isOverride ? getEffectiveVisibilityForOverriddenProperty(t,getprop,parent,objectType,fileOverviewVisibility) : getEffectiveVisibilityForNonOverriddenProperty(getprop,objectType,fileOverviewVisibility);
}
