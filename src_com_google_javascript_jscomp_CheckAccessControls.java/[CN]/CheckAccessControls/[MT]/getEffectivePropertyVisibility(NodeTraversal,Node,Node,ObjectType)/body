{
  boolean isOverride=parent.getJSDocInfo() != null && parent.isAssign() && parent.getFirstChild() == getprop;
  return isOverride ? getEffectiveVisibilityForOverriddenProperty(t,getprop,parent,objectType) : getEffectiveVisibilityForNonOverriddenProperty(getprop,objectType);
}
