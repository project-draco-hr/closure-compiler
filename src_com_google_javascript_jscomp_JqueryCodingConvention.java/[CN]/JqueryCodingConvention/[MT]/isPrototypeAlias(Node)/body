{
  if (n.isGetProp()) {
    if (n.isName() || n.isGetProp() || n.isGetElem()) {
      return !NodeUtil.isLValue(n) && prototypeAliases.contains(n.getQualifiedName());
    }
 else {
      return prototypeAliases.contains(n.getQualifiedName());
    }
  }
  return false;
}
