{
  if (n.isThis()) {
    return t.inGlobalScope();
  }
 else   if (n.isName()) {
    String varName=n.getString();
    if (varName.equals(GLOBAL_THIS_NAME)) {
      return true;
    }
  }
  return false;
}
