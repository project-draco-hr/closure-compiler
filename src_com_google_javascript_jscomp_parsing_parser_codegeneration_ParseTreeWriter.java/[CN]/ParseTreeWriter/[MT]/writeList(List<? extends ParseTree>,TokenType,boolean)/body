{
  boolean first=true;
  for (  ParseTree element : list) {
    if (first) {
      first=false;
    }
 else {
      if (delimiter != null) {
        write(delimiter);
      }
      if (writeNewLine) {
        writeln();
      }
    }
    visitAny(element);
  }
}
