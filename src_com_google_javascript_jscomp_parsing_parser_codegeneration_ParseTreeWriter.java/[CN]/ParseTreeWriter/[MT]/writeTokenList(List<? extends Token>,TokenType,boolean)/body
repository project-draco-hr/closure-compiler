{
  boolean first=true;
  for (  Token element : list) {
    if (first) {
      first=false;
    }
 else {
      if (delimiter != null) {
        write(delimiter);
      }
      if (writeNewLine) {
        writeln();
      }
    }
    write(element);
  }
}
