{
  CompilerRunner.FLAG_warning_level.setForTest(WarningLevel.QUIET);
  CompilerRunner.FLAG_jscomp_warning.setForTest(Lists.newArrayList("checkTypes"));
  test("var x = x || {}; x.f = function() {}; x.f(3);",TypeCheck.WRONG_ARGUMENT_COUNT);
}
